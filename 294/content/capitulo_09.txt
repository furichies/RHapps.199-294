# Capítulo 9. Automatización de tareas de administración de Linux

capítulo 9
Automatización de tareas de
administración de Linux
Meta
Automatizar las tareas de administración comunes
del sistema Linux con Ansible.
Objetivos
•
Suscribir sistemas, configurar canales de
software y repositorios, habilitar flujos de
módulos y gestionar paquetes RPM en hosts
gestionados.
•
Administrar usuarios y grupos de Linux,
configurar SSH y modificar la configuración de
Sudo en hosts gestionados.
•
Administrar el inicio del servicio, programar
procesos con at, cron y systemd, reiniciar los
hosts gestionados con reboot y controlar el
destino de inicio predeterminado en los hosts
gestionados.
•
Particionar los dispositivos de almacenamiento,
configurar LVM, formatear particiones o
volúmenes lógicos, montar sistemas de
archivos y agregar espacios de intercambio
(swap).
•
Configurar los ajustes de red y la resolución de
nombres en los hosts gestionados y recopilar
datos de Ansible relacionados con la red.
Secciones
•
Administración de software y suscripciones (y
ejercicio guiado)
•
Administración de usuarios y autenticación (y
ejercicio guiado)
•
Administración del proceso de arranque y
procesos programados (y ejercicio guiado)
•
Administración del almacenamiento (y ejercicio
guiado)
•
Administración de la configuración de red (y
ejercicio guiado)
RH294-RHEL9.0-es-3-20240620
405


capítulo 9 |  Automatización de tareas de administración de Linux
Trabajo de
laboratorio
•
Automatización de tareas de administración de
Linux
406
RH294-RHEL9.0-es-3-20240620


capítulo 9 |  Automatización de tareas de administración de Linux
Administración de software y
suscripciones
Objetivos
• Suscribir sistemas, configurar canales de software y repositorios, habilitar flujos de módulos y
gestionar paquetes RPM en hosts gestionados.
Administración de paquetes con Ansible
El módulo ansible.builtin.dnf de Ansible usa dnf en los hosts gestionados para manejar las
operaciones de paquetes. El siguiente playbook instala el paquete httpd en el host gestionado
servera.lab.example.com:
---
- name: Install the required packages on the web server
  hosts: servera.lab.example.com
  tasks:
    - name: Install the httpd packages
      ansible.builtin.dnf:
        name: httpd    
        state: present 
La palabra clave name especifica el nombre del paquete para instalar.
La palabra clave state indica el estado esperado del paquete en el host gestionado:
present
Ansible instala el paquete si aún no está instalado.
absent
Ansible elimina el paquete si está instalado.
latest
Ansible actualiza el paquete si aún no está en la versión más reciente disponible. Si el
paquete no está instalado, Ansible lo instala.
La siguiente tabla compara algunos usos del módulo de Ansible ansible.builtin.dnf con el
comando equivalente dnf.
Tarea de Ansible
Comando DNF
- name: Install httpd
  ansible.builtin.dnf:
    name: httpd
    state: present
dnf install httpd
RH294-RHEL9.0-es-3-20240620
407


capítulo 9 |  Automatización de tareas de administración de Linux
Tarea de Ansible
Comando DNF
- name: Install or upgrade httpd
  ansible.builtin.dnf:
    name: httpd
    state: latest
dnf upgrade httpd o dnf install
httpd si el paquete aún no está instalado.
- name: Upgrade all packages
  ansible.builtin.dnf:
    name: '*'
    state: latest
dnf upgrade
- name: Remove httpd
  ansible.builtin.dnf:
    name: httpd
    state: absent
dnf remove httpd
- name: Install Development Tools
  ansible.builtin.dnf:
    name: '@Development Tools' 
    state: present
Con el módulo de Ansible
ansible.builtin.dnf, debe agregar
a los nombres de grupos el prefijo con
el signo de arroba @. Recuerde que
puede recuperar la lista de grupos con el
comando dnf group list.
dnf group install "Development
Tools"
- name: Remove Development Tools
  ansible.builtin.dnf:
    name: '@Development Tools'
    state: absent
dnf group remove "Development
Tools"
- name: Install perl DNF module
  ansible.builtin.dnf:
    name: '@perl:5.26/minimal' 
    state: present
Para gestionar un módulo de paquete,
anteponga su nombre con el signo de
arroba (@). La sintaxis es la misma que
para el comando dnf. Por ejemplo,
puede omitir la parte del perfil para usar
el perfil predeterminado: @perl:5.26.
Recuerde que puede enumerar los
módulos de paquetes disponibles con el
comando dnf module list.
dnf module install perl:5.26/
minimal
408
RH294-RHEL9.0-es-3-20240620


capítulo 9 |  Automatización de tareas de administración de Linux
Importante
Red Hat Enterprise Linux 8 proporciona varios módulos de paquetes, pero Red Hat
Enterprise Linux 9.0 no lo hizo cuando se lanzó inicialmente. Se espera que los
módulos de paquetes para versiones adicionales de componentes de software
estén disponibles en futuras versiones menores de Red Hat Enterprise Linux.
Ejecute el comando ansible-navigator doc ansible.builtin.dnf para obtener
ejemplos de parámetros y de playbooks adicionales.
Optimización de la instalación de varios paquetes
Para operar en varios paquetes, la palabra clave name acepta una lista. El siguiente playbook
instala tres paquetes en el host gestionado servera.lab.example.com.
---
- name: Install the required packages on the web server
  hosts: servera.lab.example.com
  tasks:
    - name: Install the packages
      ansible.builtin.dnf:
        name:
          - httpd
          - mod_ssl
          - httpd-tools
        state: present
Con esta sintaxis, Ansible instala los paquetes en una sola transacción de DNF. Esto es equivalente
a ejecutar el comando dnf install httpd mod_ssl httpd-tools.
Una versión más común pero menos eficiente y más lenta de esta tarea es usar un bucle.
---
- name: Install the required packages on the web server
  hosts: servera.lab.example.com
  tasks:
    - name: Install the packages
      ansible.builtin.dnf:
        name: "{{ item }}""
        state: present
      loop:
        - httpd
        - mod_ssl
        - httpd-tools
Evite usar este método, ya que requiere que el módulo realice tres transacciones individuales, una
para cada paquete.
Recopilación de datos sobre paquetes instalados
El módulo de Ansible ansible.builtin.package_facts recopila los detalles del paquete
instalado en los hosts gestionados. Establece la variable ansible_facts['packages'] con los
detalles del paquete.
RH294-RHEL9.0-es-3-20240620
409


capítulo 9 |  Automatización de tareas de administración de Linux
El playbook siguiente llama al módulo ansible.builtin.package_facts y
el módulo ansible.builtin.debug para visualizar el contenido de la variable
ansible_facts['packages'] y la versión del paquete NetworkManager instalado.
---
- name: Display installed packages
  hosts: servera.lab.example.com
  gather_facts: false
  tasks:
    - name: Gather info on installed packages
      ansible.builtin.package_facts:
        manager: auto
    - name: List installed packages
      ansible.builtin.debug:
        var: ansible_facts['packages']
    - name: Display NetworkManager version
      ansible.builtin.debug:
        var: ansible_facts['packages']['NetworkManager'][0]['version'] 
      when: ansible_facts['packages']['NetworkManager'] is defined
La variable ansible_facts['packages'] contiene una lista para cada paquete instalado.
Cuando una lista contiene un ítem, use [0] para seleccionar ese ítem.
Cuando se ejecuta, el playbook muestra la lista de paquetes y la versión del paquete
NetworkManager:
[user@controlnode ~]$ ansible-navigator run -m stdout lspackages.yml
PLAY [Display installed packages] **********************************************
TASK [Gather info on installed packages] ***************************************
ok: [servera.lab.example.com]
TASK [List installed packages] *************************************************
ok: [servera.lab.example.com] => {
    "ansible_facts['packages']": {
        "NetworkManager": [
            {
                "arch": "x86_64",
                "epoch": 1,
                "name": "NetworkManager",
                "release": "4.el9_0",
                "source": "rpm",
                "version": "1.36.0"
            }
        ],
...output omitted...
        "zstd": [
            {
                "arch": "x86_64",
                "epoch": null,
                "name": "zstd",
410
RH294-RHEL9.0-es-3-20240620


capítulo 9 |  Automatización de tareas de administración de Linux
                "release": "2.el9",
                "source": "rpm",
                "version": "1.5.1"
            }
        ]
    }
}
TASK [Display NetworkManager version] ******************************************
ok: [servera.lab.example.com] => {
    "ansible_facts['packages']['NetworkManager'][0]['version']": "1.36.0"
}
PLAY RECAP *********************************************************************
servera.lab.example.com    : ok=3    changed=0    unreachable=0    failed=0  ...
Revisión de módulos alternativos para administrar paquetes
Para otros administradores de paquetes, Ansible generalmente proporciona un módulo dedicado.
El módulo ansible.builtin.apt usa la herramienta de paquetes APT disponible en Debian o
Ubuntu. El módulo ansible.windows.win_package puede instalar software en sistemas de
Microsoft Windows.
El playbook siguiente usa condicionales para seleccionar el módulo apropiado en un entorno
compuesto por sistemas de Red Hat Enterprise Linux que ejecuta las versiones principales 7, 8 y 9.
---
- name: Install the required packages on the web servers
  hosts: webservers
  tasks:
    - name: Install httpd on RHEL 8 and 9
      ansible.builtin.dnf:
        name: httpd
        state: present
      when:
        - "ansible_facts['distribution'] == 'RedHat'"
        - "ansible_facts['distribution_major_version'] >= '8'`"
    - name: Install httpd on RHEL 7 and earlier
      `ansible.builtin.yum:
        name: httpd
        state: present
      when:
        - "ansible_facts['distribution'] == 'RedHat'"
        - "ansible_facts['distribution_major_version'] <= `'7'`"
Como alternativa, el módulo genérico ansible.builtin.package detecta y usa
automáticamente el administrador de paquetes disponible en los hosts gestionados. Con el
módulo ansible.builtin.package, puede volver a escribir el playbook anterior de la siguiente
manera:
RH294-RHEL9.0-es-3-20240620
411


capítulo 9 |  Automatización de tareas de administración de Linux
---
- name: Install the required packages on the web servers
  hosts: webservers
  tasks:
    - name: Install httpd
      ansible.builtin.package:
        name: httpd
        state: present
Sin embargo, el módulo ansible.builtin.package no soporta todas las características que
proporcionan los módulos más especializados.
Además, los sistemas operativos a menudo tienen nombres diferentes para los paquetes que
proporcionan. Por ejemplo, el paquete que instala el servidor HTTP Apache es httpd en Red Hat
Enterprise Linux y apache2 en Ubuntu.
En ese caso, aún necesita un condicional para seleccionar el nombre correcto del paquete en
función del sistema operativo del host gestionado.
Registro y gestión de sistemas con Red Hat
Subscription Management
Puede habilitar sus sistemas Red Hat Enterprise Linux para suscripciones de productos mediante
el uso de algunos métodos diferentes:
• Puede usar el comando subscription-manager.
• En los sistemas Red Hat Enterprise Linux 9.2 y posteriores, puede usar el rol rhel-system-
roles.rhc disponible en el RPM rhel-system-roles (versión 1.21.1 y posteriores).
• Puede usar el rol redhat.rhel_system_roles.rhc de la colección
redhat.rhel_system_roles (versión 1.21.1 y posteriores).
nota
El entorno de ejecución de automatización registry.redhat.io/ansible-
automation-platform-24/ee-supported-rhel8 contiene la colección
redhat.rhel_system_roles. También puede instalar la colección
redhat.rhel_system_roles en su proyecto de Ansible y, luego, usar el entorno
de ejecución de automatización ee-supported-rhel8 disponible para Ansible
Automation Platform 2.2 o 2.3.
Gestión de Red Hat Subscription Management desde la línea
de comandos
Sin Ansible, puede usar el comando subscription-manager para registrar un sistema:
[user@host ~]$ sudo subscription-manager register
Registering to: subscription.rhsm.redhat.com:443/subscription
Username: yourusername
Password: yourpassword
...output omitted...
412
RH294-RHEL9.0-es-3-20240620


capítulo 9 |  Automatización de tareas de administración de Linux
El siguiente comando adjunta una suscripción con un ID de conjunto. Puede enumerar los
conjuntos disponibles para su cuenta con el comando subscription-manager list --
available.
[user@host ~]$ sudo subscription-manager attach --pool=poolID
Después de registrar un sistema y adjuntar una suscripción, puede usar el comando
subscription-manager para habilitar los repositorios de software de Red Hat en el sistema.
Puede usar el comando subscription-manager repos --list para identificar los
repositorios disponibles y, luego, usar el comando subscription-manager repos enable
para habilitar los repositorios:
[user@host ~]$ sudo subscription-manager repos \
> --enable "rhel-9-for-x86_64-baseos-rpms" \
> --enable "rhel-9-for-x86_64-appstream-rpms"
Gestión de Red Hat Subscription Management mediante el uso
de un rol
Ya sea que use el rol rhel-system-roles.rhc del RPM rhel-system-roles o el rol
redhat.rhel_system_roles.rhc de la colección redhat.rhel_system_roles, los pasos
para administrar la gestión de suscripciones a Red Hat son esencialmente los mismos.
1.
Cree un play que incluya el rol deseado:
---
- name: Register systems
  hosts: all
  become: true
  tasks:
    - name: Include the rhc role
      ansible.builtin.include_role:
        name: redhat.rhel_system_roles.rhc
2.
Defina las variables para el rol. Puede definir estas variables en el play, en un directorio
group_vars, en un directorio host_vars o en un archivo de variables separado:
---
rhc_state: present 
rhc_auth: 
  login:
    username: yourusername
    password: yourpassword
rhc_insights: 
  state: present
rhc_repositories: 
  - name: rhel-9-for-x86_64-baseos-rpms
    state: enabled
  - name: rhel-9-for-x86_64-appstream-rpms
    state: enabled
RH294-RHEL9.0-es-3-20240620
413


capítulo 9 |  Automatización de tareas de administración de Linux
La variable rhc_state especifica si el sistema debe estar conectado (o registrado) a
Red Hat. Los valores válidos son present, absent y reconnect. Cuando se establece
en present o reconnect, el rol intenta conectar automáticamente una suscripción.
La variable rhc_auth define variables adicionales relacionadas con la autenticación
en la gestión de suscripciones a Red Hat, como las variables rhc_auth['login'] y
rhc_auth['activation_keys']. Una opción para la autenticación es especificar su
nombre de usuario y contraseña. Si usa esta opción, puede considerar proteger estas
variables con Ansible Vault. Una segunda opción es definir claves de activación para su
organización.
La variable rhc_insights define variables adicionales relacionadas con Red Hat
Insights. De manera predeterminada, la variable rhc_insights['state']
tiene un valor de present, que habilita la integración de Red Hat Insights.
Hay variables adicionales disponibles para Red Hat Insights cuando la variable
rhc_insights['state'] tiene un valor de present. Establezca la variable
rhc_insights['state'] en absent para deshabilitar la integración de Red Hat
Insights.
La variable rhc_repositories define una lista de repositorios para habilitar o
deshabilitar.
3.
Después de crear el playbook y definir las variables para el rol, ejecute el playbook para aplicar
la configuración.
Configuración de un repositorio de paquetes RPM
Para habilitar el soporte para un repositorio Yum de terceros en un host gestionado, Ansible
proporciona el módulo ansible.builtin.yum_repository.
Configuración de un repositorio de paquetes RPM
Cuando se ejecuta, el playbook siguiente declara un nuevo repositorio Yum en
servera.lab.example.com.
---
- name: Configure the company YUM/DNF repositories
  hosts: servera.lab.example.com
  tasks:
    - name: Ensure Example Repo exists
      ansible.builtin.yum_repository:
        file: example   
        name: example-internal
        description: Example Inc. Internal YUM/DNF repo
        baseurl: http://materials.example.com/yum/repository/
        enabled: true
        gpgcheck: true   
        state: present  
La palabra clave file especifica el nombre del archivo que se creará en el directorio /etc/
yum.repos.d/. El módulo agrega automáticamente la extensión .repo a ese nombre.
Por lo general, los proveedores de software firman digitalmente paquetes RPM con claves
GPG. Al establecer la palabra clave gpgcheck en true, el sistema RPM verifica la integridad
del paquete al confirmar que el paquete fue firmado por la clave GPG apropiada. El sistema
414
RH294-RHEL9.0-es-3-20240620


capítulo 9 |  Automatización de tareas de administración de Linux
RPM no instala ningún paquete cuya firma GPG no coincide. Use el módulo de Ansible
ansible.builtin.rpm_key, descrito más adelante en esta sección, para instalar la clave
GPG pública requerida.
Al configurar la palabra clave state en present, Ansible crea o actualiza el archivo .repo.
Al configurar state en absent, Ansible elimina el archivo.
El archivo /etc/yum.repos.d/example.repo resultante en servera.lab.example.com es
de la siguiente forma:
[example-internal]
async = 1
baseurl = http://materials.example.com/yum/repository/
enabled = 1
gpgcheck = 1
name = Example Inc. Internal YUM/DNF repo
El módulo ansible.builtin.yum_repository expone la mayoría de los parámetros de
configuración del repositorio como palabras clave. Ejecute el comando ansible-navigator
doc ansible.builtin.yum_repository para obtener ejemplos de parámetros y de
playbooks adicionales.
nota
Algunos repositorios de terceros proporcionan el archivo de configuración y la
clave pública de GPG como parte del paquete de RPM que puede descargarse e
instalarse con el comando dnf install.
Por ejemplo, el proyecto Extra Packages for Enterprise Linux (EPEL) proporciona
el paquete https://dl.fedoraproject.org/pub/epel/epel-release-
latest-9.noarch.rpm que implementa el archivo de configuración /etc/
yum.repos.d/epel.repo.
Para este repositorio, use el módulo ansible.builtin.dnf para instalar el
paquete EPEL en lugar del módulo ansible.builtin.yum_repository.
Importación de una clave GPG de RPM
Cuando la palabra clave gpgcheck se establece en true en el módulo
ansible.builtin.yum_repository, también debe instalar la clave GPG en el
host gestionado. El módulo ansible.builtin.rpm_key del siguiente ejemplo
implementa la clave pública GPG alojada en un servidor web remoto en el host gestionado
servera.lab.example.com.
---
- name: Configure the company YUM/DNF repositories
  hosts: servera.lab.example.com
  tasks:
    - name: Deploy the GPG public key
      ansible.builtin.rpm_key:
        key: http://materials.example.com/yum/repository/RPM-GPG-KEY-example
        state: present
    - name: Ensure Example Repo exists
RH294-RHEL9.0-es-3-20240620
415


capítulo 9 |  Automatización de tareas de administración de Linux
      ansible.builtin.yum_repository:
        file: example
        name: example-internal
        description: Example Inc. Internal YUM/DNF repo
        baseurl: http://materials.example.com/yum/repository/
        enabled: true
        gpgcheck: true
        state: present
Referencias
Páginas del manual: dnf(8), yum.conf(5) y subscription-manager(8)
ansible.builtin.dnf module - Manages Packages with the DNF Package
Manager - Ansible Documentation
https://docs.ansible.com/ansible/latest/collections/ansible/builtin/
dnf_module.html
ansible.builtin.package_facts module - Package Information as Facts - Ansible
Documentation
https://docs.ansible.com/ansible/latest/collections/ansible/builtin/
package_facts_module.html
Introduction to the rhel-system-roles.rhc Role
https://access.redhat.com/documentation/en-us/red_hat_enterprise_linux/9/
html-single/automating_system_administration_by_using_rhel_system_roles/
index#using-the-rhc-system-role-to-register-the-system_automating-system-
administration-by-using-rhel-system-roles
Using the redhat.rhel_system_roles.rhc Collection Role
https://console.redhat.com/ansible/automation-hub/repo/published/redhat/
rhel_system_roles/content/role/rhc/
ansible.builtin.yum_repository module - Add or Remove YUM
Repositories - Ansible Documentation
https://docs.ansible.com/ansible/latest/collections/ansible/builtin/
yum_repository_module.html
ansible.builtin.rpm_key module - Adds or Removes a GPG Key from the RPM
DB - Ansible Documentation
https://docs.ansible.com/ansible/latest/collections/ansible/builtin/
rpm_key_module.html
416
RH294-RHEL9.0-es-3-20240620


capítulo 9 |  Automatización de tareas de administración de Linux
Ejercicio Guiado
Administración de software y
suscripciones
Configurar un nuevo repositorio Yum y, desde allí, instala paquetes en sus hosts gestionados.
Resultados
• Configurar un repositorio Yum con el módulo ansible.builtin.yum_repository.
• Administrar las claves GPG de RPM con el módulo ansible.builtin.rpm_key.
• Obtener información sobre los paquetes instalados en un host con el módulo
ansible.builtin.package_facts.
Antes De Comenzar
Con el usuario student en la máquina workstation, use el comando lab para preparar su
sistema para este ejercicio.
Este comando prepara su entorno y garantiza que estén disponibles todos los recursos
requeridos.
[student@workstation ~]$ lab start system-software
Instrucciones
Su organización requiere que el paquete RPM simple-agent esté instalado en todos los hosts.
Este paquete lo proporciona un repositorio Yum interno que mantiene su organización para alojar
paquetes de software desarrollados internamente.
Debe escribir un playbook para asegurarse de que el paquete simple-agent esté instalado
en todos los hosts gestionados. El playbook también debe garantizar que todos los hosts
gestionados estén configurados para usar el repositorio Yum interno.
El repositorio se encuentra en http://materials.example.com/yum/repository. Todos
los paquetes RPM en el repositorio están firmados con un par de claves GPG. La clave pública de
GPG para los paquetes del repositorio está disponible en http://materials.example.com/
yum/repository/RPM-GPG-KEY-example.
 1.
Cambie al directorio /home/student/system-software.
[student@workstation ~]$ cd ~/system-software
[student@workstation system-software]$
 2.
Empiece a escribir el playbook repo_playbook.yml. Defina un solo play en el playbook
que se dirige a todos los hosts. Agregue una cláusula vars al play que defina una sola
variable, custom_pkg, con el valor simple-agent (el nombre del paquete RPM que debe
instalarse en todas partes). Agregue una cláusula tasks vacía al play.
RH294-RHEL9.0-es-3-20240620
417


capítulo 9 |  Automatización de tareas de administración de Linux
El playbook debería incluir el siguiente contenido:
---
- name: Repository Configuration
  hosts: all
  vars:
    custom_pkg: simple-agent
  tasks:
 3.
Agregue dos tareas a la cláusula tasks del play en el archivo repo_playbook.yml.
Use el módulo ansible.builtin.package_facts de la primera tarea para recolectar
información sobre los paquetes instalados en los hosts gestionados. Esta tarea completa el
dato ansible_facts.packages.
Use el módulo ansible.builtin.debug en la segunda tarea para imprimir la versión
instalada del paquete al que hace referencia la variable custom_pkg. Solo ejecute esta
tarea si el paquete personalizado se encuentra en el dato ansible_facts.packages.
Ejecute el playbook repo_playbook.yml.
3.1.
Agregue la primera tarea al play. Establezca el valor de la palabra clave manager en
auto para el módulo ansible.builtin.package_facts.
La primera tarea debe contar con el siguiente contenido:
    - name: Gather Package Facts
      ansible.builtin.package_facts:
        manager: auto
3.2.
Agregue una segunda tarea al play que usa el módulo ansible.builtin.debug
para mostrar el valor de la variable ansible_facts.packages[custom_pkg].
Agregue una cláusula when a la tarea para verificar que el valor de la variable
custom_pkg está contenido en la variable ansible_facts['packages'].
La segunda tarea debe incluir el siguiente contenido:
    - name: Show Package Facts for the custom package
      ansible.builtin.debug:
        var: ansible_facts['packages'][custom_pkg]
      when: custom_pkg in ansible_facts['packages']
3.3.
Ejecute el playbook:
[student@workstation system-software]$ ansible-navigator run \
> -m stdout repo_playbook.yml
PLAY [Repository Configuration] **********************************************
TASK [Gathering Facts] *******************************************************
ok: [servera.lab.example.com]
TASK [Gather Package Facts] **************************************************
ok: [servera.lab.example.com]
TASK [Show Package Facts for the custom package] *****************************
418
RH294-RHEL9.0-es-3-20240620


capítulo 9 |  Automatización de tareas de administración de Linux
skipping: [servera.lab.example.com]
PLAY RECAP *******************************************************************
servera.lab.example.com    : ok=2    changed=0    unreachable=0    failed=0   
 skipped=1    rescued=0    ignored=0
La tarea Show Package Facts for the custom package se omite porque el
paquete simple-agent no está instalado en los hosts gestionados.
 4.
Agregue una tercera tarea al play que use el módulo
ansible.builtin.yum_repository para asegurar la configuración del repositorio yum
interno en los hosts gestionados. Esta tarea tiene los siguientes requisitos:
• La configuración del repositorio se almacena en el archivo /etc/yum.repos.d/
example.repo
• El ID del repositorio es example-internal
• La URL base del repositorio es http://materials.example.com/yum/
repository
• El repositorio está configurado para verificar las firmas RPM GPG.
• La descripción del repositorio es Example Inc. Internal YUM repo
La tercera tarea debe contar con el siguiente contenido:
    - name: Ensure Example Repo exists
      ansible.builtin.yum_repository:
        name: example-internal
        description: Example Inc. Internal YUM repo
        file: example
        baseurl: http://materials.example.com/yum/repository/
        gpgcheck: true
 5.
Agregue una cuarta tarea al play que use el módulo ansible.builtin.rpm_key para
asegurarse de que la clave pública del repositorio esté presente en los hosts gestionados.
La clave pública del repositorio está disponible en http://materials.example.com/
yum/repository/RPM-GPG-KEY-example.
La cuarta tarea debe contar con el siguiente contenido:
    - name: Ensure Repo RPM Key is Installed
      ansible.builtin.rpm_key:
        key: http://materials.example.com/yum/repository/RPM-GPG-KEY-example
        state: present
 6.
Agregue una quinta tarea al play para asegurarse de que el paquete al que hace referencia
la variable custom_pkg esté instalado en los hosts gestionados.
La quinta tarea debe contar con el siguiente contenido:
    - name: Install Example package
      ansible.builtin.dnf:
        name: "{{ custom_pkg }}"
        state: present
RH294-RHEL9.0-es-3-20240620
419


capítulo 9 |  Automatización de tareas de administración de Linux
 7.
El dato ansible_facts['packages'] no se actualiza automáticamente cuando se
instala un nuevo paquete en un host gestionado. Este paso demuestra que esto es cierto.
Copie la segunda tarea y agréguela como la sexta tarea en el play. Ejecute el playbook y
verifique que el dato ansible_facts['packages'] no contenga información sobre el
paquete simple-agent instalado en los hosts gestionados.
7.1.
La sexta tarea contiene una copia de la segunda tarea:
    - name: Show Package Facts for the custom package
      ansible.builtin.debug:
        var: ansible_facts['packages'][custom_pkg]
      when: custom_pkg in ansible_facts['packages']
El playbook repo_playbook.yml completo debería incluir el siguiente contenido:
---
- name: Repository Configuration
  hosts: all
  vars:
    custom_pkg: simple-agent
  tasks:
    - name: Gather Package Facts
      ansible.builtin.package_facts:
        manager: auto
    - name: Show Package Facts for the custom package
      ansible.builtin.debug:
        var: ansible_facts['packages'][custom_pkg]
      when: custom_pkg in ansible_facts['packages']
    - name: Ensure Example Repo exists
      ansible.builtin.yum_repository:
        name: example-internal
        description: Example Inc. Internal YUM repo
        file: example
        baseurl: http://materials.example.com/yum/repository/
        gpgcheck: true
    - name: Ensure Repo RPM Key is Installed
      ansible.builtin.rpm_key:
        key: http://materials.example.com/yum/repository/RPM-GPG-KEY-example
        state: present
    - name: Install Example package
      ansible.builtin.dnf:
        name: "{{ custom_pkg }}"
        state: present
    - name: Show Package Facts for the custom package
      ansible.builtin.debug:
        var: ansible_facts['packages'][custom_pkg]
      when: custom_pkg in ansible_facts['packages']
7.2.
Ejecute el playbook.
420
RH294-RHEL9.0-es-3-20240620


capítulo 9 |  Automatización de tareas de administración de Linux
[student@workstation system-software]$ ansible-navigator run \
> -m stdout repo_playbook.yml
PLAY [Repository Configuration] **********************************************
TASK [Gathering Facts] *******************************************************
ok: [servera.lab.example.com]
TASK [Gather Package Facts] **************************************************
ok: [servera.lab.example.com]
TASK [Show Package Facts for the custom package] *****************************
skipping: [servera.lab.example.com]
TASK [Ensure Example Repo exists] ********************************************
changed: [servera.lab.example.com]
TASK [Ensure Repo RPM Key is Installed] **************************************
changed: [servera.lab.example.com]
TASK [Install Example package] ******************************************
changed: [servera.lab.example.com]
TASK [Show Package Facts for the custom package] *****************************
skipping: [servera.lab.example.com]
PLAY RECAP *******************************************************************
servera.lab.example.com    : ok=5    changed=3    unreachable=0    failed=0   
 skipped=2    rescued=0    ignored=0
La tarea Gather Package Facts determina los datos contenidos en el dato
ansible_facts['packages'].
La tarea se omite porque el paquete simple-agent se instala después de la
tarea Gather Package Facts.
 8.
Actualice los datos del paquete en su play insertando una tarea inmediatamente después
de la tarea Install Example package. Escriba la nueva tarea para que ejecute el
módulo ansible.builtin.package_facts. Establezca el atributo manager del
módulo en auto.
El playbook completo debería incluir el siguiente contenido:
---
- name: Repository Configuration
  hosts: all
  vars:
    custom_pkg: simple-agent
  tasks:
    - name: Gather Package Facts
      ansible.builtin.package_facts:
        manager: auto
    - name: Show Package Facts for the custom package
RH294-RHEL9.0-es-3-20240620
421


capítulo 9 |  Automatización de tareas de administración de Linux
      ansible.builtin.debug:
        var: ansible_facts['packages'][custom_pkg]
      when: custom_pkg in ansible_facts['packages']
    - name: Ensure Example Repo exists
      ansible.builtin.yum_repository:
        name: example-internal
        description: Example Inc. Internal YUM repo
        file: example
        baseurl: http://materials.example.com/yum/repository/
        gpgcheck: true
    - name: Ensure Repo RPM Key is Installed
      ansible.builtin.rpm_key:
        key: http://materials.example.com/yum/repository/RPM-GPG-KEY-example
        state: present
    - name: Install Example package
      ansible.builtin.dnf:
        name: "{{ custom_pkg }}"
        state: present
    - name: Gather Package Facts
      ansible.builtin.package_facts:
        manager: auto
    - name: Show Package Facts for the custom package
      ansible.builtin.debug:
        var: ansible_facts['packages'][custom_pkg]
      when: custom_pkg in ansible_facts['packages']
 9.
Use un comando ad hoc Ansible para eliminar el paquete simple-agent
instalado durante la ejecución previa del playbook. Ejecute el playbook con la tarea
ansible.builtin.package_facts insertada y use la salida para verificar la instalación
del paquete simple-agent.
9.1.
Para eliminar el paquete simple-agent de todos los hosts, use el comando
ansible all con las opciones -m ansible.builtin.dnf y -a
'name=simple-agent state=absent'.
[student@workstation system-software]$ ansible all -m ansible.builtin.dnf \
> -a 'name=simple-agent state=absent'
servera.lab.example.com | CHANGED => {
...output omitted...
    "changed": true,
    "msg": "",
    "rc": 0,
    "results": [
        "Removed: simple-agent-1.0-1.el9.x86_64"
    ]
...output omitted...
9.2.
Ejecute el playbook repo_playbook.yml.
422
RH294-RHEL9.0-es-3-20240620


capítulo 9 |  Automatización de tareas de administración de Linux
[student@workstation system-software]$ ansible-navigator run \
> -m stdout repo_playbook.yml
PLAY [Repository Configuration] **********************************************
TASK [Gathering Facts] *******************************************************
ok: [servera.lab.example.com]
TASK [Gather Package Facts] **************************************************
ok: [servera.lab.example.com]
TASK [Show Package Facts for the custom package] *****************************
skipping: [servera.lab.example.com]
...output omitted...
TASK [Install Example package] ***********************************************
changed: [servera.lab.example.com]
TASK [Gather Package Facts] **************************************************
ok: [servera.lab.example.com]
TASK [Show Package Facts for the custom package] *****************************
ok: [servera.lab.example.com] => {
    "ansible_facts['packages'][custom_pkg]": [
        {
            "arch": "x86_64",
            "epoch": null,
            "name": "simple-agent",
            "release": "1.el9",
            "source": "rpm",
            "version": "1.0"
        }
    ]
}
PLAY RECAP *******************************************************************
servera.lab.example.com    : ok=7    changed=1    unreachable=0    failed=0   
 skipped=1    rescued=0    ignored=0
No existe un dato de paquete para el paquete simple-agent porque el
paquete no está instalado en los hosts gestionados.
El paquete simple-agent se instala como resultado de esta tarea, como lo
indica el estado changed.
Esta tarea actualiza los datos del paquete con información sobre el paquete
simple-agent.
El dato de paquete simple-agent existe e indica que solo un paquete
simple-agent está instalado. El paquete instalado es la versión 1.0.
RH294-RHEL9.0-es-3-20240620
423


capítulo 9 |  Automatización de tareas de administración de Linux
Finalizar
En la máquina workstation, cambie al directorio de inicio de usuario student y use el comando
lab para completar este ejercicio. Este paso es importante para garantizar que los recursos de
ejercicios anteriores no impacten en los siguientes.
[student@workstation ~]$ lab finish system-software
424
RH294-RHEL9.0-es-3-20240620


capítulo 9 |  Automatización de tareas de administración de Linux
Administración de usuarios y
autenticación
Objetivos
• Gestionar usuarios y grupos de Linux, configurar SSH y modificar la configuración de Sudo en
hosts gestionados.
El módulo user (usuario)
El módulo Ansible ansible.builtin.user le permite crear, configurar y eliminar cuentas
de usuario en hosts gestionados. Puede quitar o agregar un usuario, configurar el directorio
de inicio de un usuario, configurar el UID para las cuentas de usuario del sistema, gestionar las
contraseñas y asignar un usuario a grupos complementarios. Para crear un usuario que pueda
iniciar sesión en la máquina, debe proporcionar una contraseña con hash para el parámetro de
contraseña. Consulte "How do I generate encrypted passwords for the user module?" [https://
docs.ansible.com/ansible/latest/reference_appendices/faq.html#how-do-i-generate-encrypted-
passwords-for-the-user-module] para obtener información sobre cómo aplicar un hash a una
contraseña.
En el siguiente ejemplo se demuestra el módulo ansible.builtin.user:
- name: Create devops_user if missing, make sure is member of correct groups
  ansible.builtin.user:
    name: devops_user 
    shell: /bin/bash 
    groups: sys_admins, developers 
    append: true
El parámetro name es la única opción requerida por el módulo ansible.builtin.user.
Su valor es el nombre de la cuenta de servicio o la cuenta de usuario para crear, eliminar o
modificar.
El parámetro shell establece el shell del usuario.
El parámetro groups, cuando se usa con el parámetro append, le dice a la máquina que
agregue los grupos suplementarios sys_admins y developers a este usuario. Si no usa
el parámetro append, los grupos proporcionados sobrescriben los grupos complementarios
existentes de un usuario. Para configurar el grupo principal para un usuario, use la opción
group.
nota
El módulo ansible.builtin.user también proporciona información en valores
de retorno, como el directorio de inicio del usuario y una lista de grupos de los que
el usuario es miembro. Estos valores de retorno pueden registrarse en una variable
y usarse en tareas posteriores. Puede obtener más información disponible en la
documentación para el módulo.
RH294-RHEL9.0-es-3-20240620
425


capítulo 9 |  Automatización de tareas de administración de Linux
Parámetros de uso común para el módulo de usuario
Parámetro
Comentarios
comment
Opcionalmente, establece la descripción de una cuenta de
usuario.
group
Opcionalmente, establece el grupo primario del usuario.
groups
Opcionalmente, establece una lista de grupos
complementarios para el usuario. Cuando se establece en
un valor nulo, se eliminan todos los grupos excepto el grupo
primario.
home
Opcionalmente, establece la ubicación del directorio de inicio
del usuario.
create_home
Opcionalmente, toma un valor booleano de true o false.
Se crea un directorio de inicio para el usuario si el valor se
establece en true.
system
Opcionalmente, toma un valor booleano de true o false.
Al crear una cuenta, convierte al usuario en una cuenta del
sistema si el valor está definido en true. Esta configuración
no se puede cambiar en los usuarios existentes.
uid
Establece el número de UID del usuario.
state
Si se establece en present, cree la cuenta si falta (la
configuración predeterminada). Si se establece en absent,
elimine la cuenta si está presente.
Use el módulo de usuario para generar una clave SSH
El módulo ansible.builtin.user puede generar una clave SSH si se invoca con el parámetro
generate_ssh_key.
En el siguiente ejemplo se demuestra cómo el módulo ansible.builtin.user genera una
clave SSH:
- name: Create an SSH key for user1
  ansible.builtin.user:
    name: user1
    generate_ssh_key: true 
    ssh_key_bits: 2048 
    ssh_key_file: .ssh/id_my_rsa 
El parámetro generate_ssh_key acepta un valor booleano que especifica si se debe
generar una clave SSH para el usuario. Esto no sobrescribe una clave SSH existente a menos
que el parámetro force se proporcione con el valor true.
El parámetro ssh_key_bits establece el número de bits en la nueva clave SSH.
El parámetro ssh_key_file especifica el nombre de archivo para la nueva clave privada
SSH (la clave pública agrega el sufijo .pub).
426
RH294-RHEL9.0-es-3-20240620


capítulo 9 |  Automatización de tareas de administración de Linux
El módulo group
El módulo ansible.builtin.group agrega, elimina y modifica grupos en los hosts
gestionados. Los hosts gestionados deben tener disponibles los comandos groupadd, groupdel
y groupmod, que se proporcionan en el paquete shadow-utils en Red Hat Enterprise Linux 9.
Para los hosts gestionados de Microsoft Windows, use el módulo win_group.
En el siguiente ejemplo se demuestra cómo el módulo ansible.builtin.group crea un grupo:
- name: Verify that the auditors group exists
  ansible.builtin.group:
    name: auditors 
    state: present 
El parámetro name es la única opción requerida por el módulo ansible.builtin.group.
El valor es el nombre del grupo que se administrará.
El parámetro state acepta el valor absent o present, que elimina o crea el grupo,
respectivamente. El valor predeterminado para el parámetro state es present.
Parámetros para el módulo group
Parámetro
Comentarios
gid
Este parámetro establece el número GID para el grupo. Si se
omite, el número se selecciona automáticamente.
local
Este parámetro fuerza el uso de alternativas de comandos
locales (en lugar de comandos que pueden cambiar las
fuentes de autenticación central) en las plataformas que lo
implementan.
name
Este parámetro define el nombre del grupo que se
administrará.
state
Este parámetro determina si el grupo debe ser present o
absent en el host remoto.
system
Si este parámetro se establece en true, el grupo se crea
como un grupo de sistema (normalmente, con un número GID
por debajo de 1000).
El módulo known hosts (host conocido)
El módulo ansible.builtin.known_hosts administra las claves de host SSH al agregarlas o
eliminarlas en los hosts gestionados. Esto garantiza que los hosts gestionados puedan establecer
automáticamente la autenticidad de las conexiones SSH con otros hosts gestionados, lo
que garantiza que no se solicite a los usuarios que verifiquen la huella digital SSH de un host
gestionado remoto la primera vez que se conectan a él.
En el siguiente ejemplo se demuestra cómo el módulo ansible.builtin.known_hosts copia
una clave de host en un host gestionado:
RH294-RHEL9.0-es-3-20240620
427


capítulo 9 |  Automatización de tareas de administración de Linux
- name: Copy host keys to remote servers
  ansible.builtin.known_hosts:
    path: /etc/ssh/ssh_known_hosts 
    name: servera.lab.example.com 
    key: servera.lab.example.com,172.25.250.10 ssh-rsa ASDeararAIUHI324324 
El parámetro path especifica la ruta al archivo known_hosts para editar. Si el archivo no
existe, se crea.
El parámetro name especifica el nombre del host para agregar o eliminar. El nombre debe
coincidir con el nombre de host o la dirección IP de la clave que se agrega.
El parámetro key es la clave de host pública SSH como una cadena en un formato específico.
Por ejemplo, el valor del parámetro key debe tener el formato <hostname[,IP]> ssh-
rsa <pubkey> para una clave de host pública RSA (que se encuentra en el archivo de clave
/etc/ssh/ssh_host_rsa_key.pub de un host) o <hostname[,IP]> ssh-ed25519
<pubkey> para una clave de host pública Ed25519 (que se encuentra en el archivo de clave /
etc/ssh/ssh_host_ed25519_key.pub de un host).
En el siguiente ejemplo se demuestra cómo usar el complemento (plug-in) lookup para
completar el parámetro key de un archivo existente en el proyecto de Ansible:
- name: Copy host keys to remote servers
  ansible.builtin.known_hosts:
    path: /etc/ssh/ssh_known_hosts
    name: serverb
    key: "{{ lookup('ansible.builtin.file', 'pubkeys/serverb') }}" 
Esta expresión Jinja2 usa la función lookup con el complemento (plug-in) de búsqueda
ansible.builtin.file para cargar el contenido del archivo de claves pubkeys/
serverb del proyecto Ansible como el valor de la opción key. Puede enumerar los
complementos (plug-ins) de búsqueda disponibles mediante el comando ansible-
navigator doc -l -t lookup.
El siguiente play es un ejemplo que usa algunas técnicas avanzadas para construir un archivo
/etc/ssh/ssh_known_hosts para todos los hosts gestionados en el inventario. Puede
haber formas más eficientes de lograr esto, ya que ejecuta un bucle anidado en todos los hosts
gestionados.
Usa el módulo ansible.builtin.slurp para obtener el contenido de las claves de host
públicas RSA y Ed25519 SSH en formato Base64 y, luego, procesa los valores de la variable
registrada con los filtros b64decode y trim para volver a convertir esos valores en texto sin
formato.
- name: Configure /etc/ssh/ssh_known_hosts files
  hosts: all
  tasks:
    - name: Collect RSA keys
      ansible.builtin.slurp:
        src: /etc/ssh/ssh_host_rsa_key.pub
      register: rsa_host_keys
    - name: Collect Ed25519 keys
428
RH294-RHEL9.0-es-3-20240620


capítulo 9 |  Automatización de tareas de administración de Linux
      ansible.builtin.slurp:
        src: /etc/ssh/ssh_host_ed25519_key.pub
      register: ed25519_host_keys
    - name: Deploy known_hosts
      ansible.builtin.known_hosts:
        path: /etc/ssh/ssh_known_hosts
        name: "{{ item[0] }}" 
        key: "{{ hostvars[ item[0] ]['ansible_facts']['fqdn'] }}
 {{ hostvars[ item[0] ][ item[1] ]['content'] | b64decode | trim }}" 
        state: present
      with_nested:
        - "{{ ansible_play_hosts }}" 
        - [ 'rsa_host_keys', 'ed25519_host_keys' ] 
item[0] es un nombre de host de inventario de la lista en la variable
ansible_play_hosts.
item[1] es la cadena rsa_host_keys o ed25519_host_keys. El filtro b64decode
convierte el valor almacenado en la variable de Base64 a texto sin formato y el filtro trim
elimina una nueva línea innecesaria. Todo esto es una línea que comienza con key y hay un
solo espacio entre las dos expresiones Jinja2.
ansible_play_hosts es una lista de los hosts que quedan en el play en este punto,
extraídos del inventario y eliminando hosts con tareas fallidas. El play debe recuperar las
claves de host públicas RSA y Ed25519 para cada uno de los otros hosts cuando construye el
archivo known_hosts en cada host del play.
Esta es una lista de dos ítems de las dos variables que usa el play para almacenar claves de
host.
nota
Los filtros y complementos (plug-ins) de búsqueda se tratan en más detalle
en el curso DO374: Desarrollo de automatización avanzada con Red Hat Ansible
Automation Platform.
El módulo authorized key (clave autorizada)
El módulo ansible.posix.authorized_key administra las claves SSH autorizadas para las
cuentas de usuario en los hosts gestionados.
En el siguiente ejemplo se demuestra cómo usar el módulo ansible.posix.authorized_key
para agregar una clave SSH a un host gestionado:
- name: Set authorized key
  ansible.posix.authorized_key:
    user: user1 
    state: present 
    key: "{{ lookup('ansible.builtin.file', 'files/user1/id_rsa.pub') }}" 
El parámetro user especifica el nombre de usuario del usuario cuyo archivo
authorized_keys se modifica en el host gestionado.
RH294-RHEL9.0-es-3-20240620
429


capítulo 9 |  Automatización de tareas de administración de Linux
El parámetro state acepta el valor present o absent con present como valor
predeterminado.
El parámetro key especifica la clave pública SSH para agregar o eliminar. En este ejemplo, la
función lookup usa el complemento (plug-in) de búsqueda ansible.builtin.file para
cargar el contenido del archivo files/user1/id_rsa.pub del proyecto Ansible como el
valor de key. Como alternativa, puede proporcionar una URL a un archivo de clave pública
como este valor.
Configuración del acceso Sudo para usuarios y grupos
En Red Hat Enterprise Linux 9, puede configurar el acceso para que un usuario o grupo ejecute
comandos sudo sin requerir una solicitud de contraseña.
En el siguiente ejemplo se demuestra cómo usar el módulo ansible.builtin.lineinfile
para proporcionar a un grupo acceso sudo a la cuenta root sin solicitar una contraseña a los
miembros del grupo:
- name: Modify sudo to allow the group01 group sudo without a password
  ansible.builtin.lineinfile:
    path: /etc/sudoers.d/group01 
    state: present 
    create: true 
    mode: 0440 
    line: "%group01 ALL=(ALL) NOPASSWD: ALL" 
    validate: /usr/sbin/visudo -cf %s 
El parámetro path especifica el archivo a modificar en el directorio /etc/sudoers.d/. Es
una buena práctica hacer coincidir el nombre del archivo con el nombre del usuario o grupo al
que está proporcionando acceso. Esto hace que sea más fácil de consultar en el futuro.
El parámetro state acepta el valor present o absent. El valor predeterminado es
present .
El parámetro create toma un valor booleano y especifica si el archivo debe crearse si aún no
existe. El valor predeterminado para el parámetro create es false.
El parámetro mode especifica los permisos en el archivo sudoers.
El parámetro line especifica la línea para agregar al archivo. El formato es específico y se
puede encontrar un ejemplo en el archivo /etc/sudoers en el comentario "Lo mismo, pero
sin contraseña". Si está configurando el acceso sudo para un grupo, debe agregar un signo
de porcentaje (%) al principio del nombre del grupo. Si está configurando el acceso sudo para
un usuario, no agregue el signo de porcentaje.
El parámetro validate especifica el comando que se ejecutará para verificar que el archivo
sea correcto. Cuando el parámetro validate está presente, el archivo se crea en una ruta
de archivo temporal y el comando proporcionado valida el archivo temporal. Si el comando de
validación se realiza correctamente, el archivo temporal se copia en la ruta especificada en el
parámetro path y se elimina el archivo temporal.
Se puede encontrar un ejemplo del comando de validación sudo en la sección de ejemplos de la
salida del comando ansible-navigator doc ansible.builtin.lineinfile.
430
RH294-RHEL9.0-es-3-20240620


capítulo 9 |  Automatización de tareas de administración de Linux
Referencias
Documentación de Ansible del módulo users
http://docs.ansible.com/ansible/latest/modules/user_module.html#user-module
¿Cómo genero contraseñas cifradas para el módulo user?
https://docs.ansible.com/ansible/latest/reference_appendices/faq.html#how-do-i-
generate-encrypted-passwords-for-the-user-module
Documentación de Ansible del módulo group
https://docs.ansible.com/ansible/latest/modules/group_module.html#group-
module
Documentación de Ansible del módulo SSH known hosts
https://docs.ansible.com/ansible/latest/modules/
known_hosts_module.html#known-hosts-module
Documentación de Ansible del módulo Authorized Key
https://docs.ansible.com/ansible/latest/modules/
authorized_key_module.html#authorized-key-module
Documentación de Ansible del complemento (plug-in) de búsqueda
https://docs.ansible.com/ansible/latest/plugins/lookup.html?highlight=lookup
Uso de filtros para manipular datos
https://docs.ansible.com/ansible/latest/user_guide/playbooks_filters.html
Documentación de Ansible del módulo Line in File
https://docs.ansible.com/ansible/latest/collections/ansible/builtin/
lineinfile_module.html
RH294-RHEL9.0-es-3-20240620
431


capítulo 9 |  Automatización de tareas de administración de Linux
Ejercicio Guiado
Administración de usuarios y
autenticación
Administrar usuarios y grupos, ajustar la configuración Sudo y configurar SSH en sus hosts
gestionados.
Resultados
• Crear un nuevo grupo de usuarios.
• Administrar usuarios mediante el módulo ansible.builtin.user.
• Completar claves SSH autorizadas con el módulo ansible.posix.authorized_key.
• Modificar el archivo /etc/ssh/sshd_config y un archivo de configuración en /etc/
sudoers.d usando el módulo ansible.builtin.lineinfile.
Antes De Comenzar
Con el usuario student en la máquina workstation, use el comando lab para preparar su
sistema para este ejercicio.
Este comando prepara su entorno y garantiza que estén disponibles todos los recursos
requeridos.
[student@workstation ~]$ lab start system-users
Instrucciones
Su organización requiere que todos los hosts tengan los mismos usuarios locales disponibles.
Estos usuarios deben pertenecer al grupo de usuarios webadmin, que tiene la capacidad de usar
el comando sudo sin especificar una contraseña. Además, las claves públicas SSH de los usuarios
deben distribuirse en el entorno para sus archivos ~/.ssh/authorized_keys para permitir la
autenticación basada en claves y no debe permitirse al usuario root iniciar sesión usando SSH
directamente.
Escriba un playbook para asegurarse de que los usuarios especificados en el archivo /home/
student/system-users/vars/users_vars.yml y el grupo de usuarios webadmin estén
presentes en los hosts gestionados y cumplan con los criterios anteriores.
 1.
Cambie al directorio /home/student/system-users.
[student@workstation ~]$ cd ~/system-users
[student@workstation system-users]$
 2.
Examine el archivo de variable vars/users_vars.yml existente.
432
RH294-RHEL9.0-es-3-20240620


capítulo 9 |  Automatización de tareas de administración de Linux
---
users:
  - username: user1
    groups: webadmin
  - username: user2
    groups: webadmin
  - username: user3
    groups: webadmin
  - username: user4
    groups: webadmin
  - username: user5
    groups: webadmin
Usa el nombre de variable username para establecer el nombre de usuario correcto y la
variable groups para definir grupos complementarios a los que el usuario debe pertenecer.
 3.
Empiece a escribir el playbook /home/student/system-users/users.yml. Defina un
solo play en el playbook que se dirija al grupo de hosts webservers.
Agregue una cláusula vars_files que cargue las variables en el archivo vars/
users_vars.yml, que se ha creado para usted y que contiene todos los nombres de
usuario necesarios para este ejercicio.
Agregue la cláusula tasks al playbook.
El playbook users.yml debería incluir el siguiente contenido:
---
- name: Create multiple local users
  hosts: webservers
  vars_files:
    - vars/users_vars.yml
  tasks:
 4.
Agregue dos tareas al play.
Cree el grupo de usuarios webadmin en los hosts gestionados mediante el módulo
ansible.builtin.group en la primera tarea.
Cree los usuarios especificados en el archivo vars/users_vars.yml usando el módulo
ansible.builtin.user para recorrer esas variables en la segunda tarea.
Ejecute el playbook users.yml.
4.1.
Agregue la primera tarea al play para asegurarse de que el grupo webadmin exista en
sus hosts gestionados.
La primera tarea debe contar con el siguiente contenido:
    - name: Add webadmin group
      ansible.builtin.group:
        name: webadmin
        state: present
4.2.
Agregue una segunda tarea al play que use el módulo ansible.builtin.user
para asegurarse de que los usuarios existan en sus hosts gestionados.
RH294-RHEL9.0-es-3-20240620
433


capítulo 9 |  Automatización de tareas de administración de Linux
Agregue una cláusula loop: "{{ users }}" a la tarea para iterar sobre cada
usuario encontrado en el archivo vars/users_vars.yml.
Para el parámetro name para los usuarios, use item['username'], no item. Esto
permite que cada ítem de la lista en el archivo de variables se estructure como un
diccionario de múltiples variables que incluye información adicional sobre cada
usuario en la lista, específicamente de qué grupos complementarios el usuario debe
ser miembro.
El playbook completo debería incluir el siguiente contenido:
---
- name: Create multiple local users
  hosts: webservers
  vars_files:
    - vars/users_vars.yml
  tasks:
    - name: Add webadmin group
      ansible.builtin.group:
        name: webadmin
        state: present
    - name: Create user accounts
      ansible.builtin.user:
        name: "{{ item['username'] }}"
        groups: "{{ item['groups'] }}"
      loop: "{{ users }}"
4.3.
Ejecute el playbook users.yml:
[student@workstation system-users]$ ansible-navigator run \
> -m stdout users.yml
PLAY [Create multiple local users] *******************************************
TASK [Gathering Facts] *******************************************************
ok: [servera.lab.example.com]
TASK [Add webadmin group] ****************************************************
changed: [servera.lab.example.com]
TASK [Create user accounts] **************************************************
changed: [servera.lab.example.com] => (item={'username': 'user1', 'groups':
 'webadmin'})
changed: [servera.lab.example.com] => (item={'username': 'user2', 'groups':
 'webadmin'})
changed: [servera.lab.example.com] => (item={'username': 'user3', 'groups':
 'webadmin'})
changed: [servera.lab.example.com] => (item={'username': 'user4', 'groups':
 'webadmin'})
changed: [servera.lab.example.com] => (item={'username': 'user5', 'groups':
 'webadmin'})
434
RH294-RHEL9.0-es-3-20240620


capítulo 9 |  Automatización de tareas de administración de Linux
PLAY RECAP *******************************************************************
servera.lab.example.com    : ok=3    changed=2    unreachable=0    failed=0   
 skipped=0    rescued=0    ignored=0
 5.
Asegúrese de que las claves públicas SSH se hayan distribuido correctamente
en el host remoto agregando una tercera tarea al play que use el módulo
ansible.posix.authorized_key.
En el directorio files, cada usuario tiene un único archivo de clave pública SSH. El
módulo recorre en bucle la lista de usuarios, encuentra la clave apropiada usando la variable
username y envía la clave al host gestionado.
Para el parámetro key, use la siguiente expresión Jinja2 para su valor para evaluar el
contenido del archivo de clave pública apropiado. Se usa una función de búsqueda con
el complemento (plug-in) file para leer el archivo y su nombre de archivo se construye
usando operaciones de cadena.
"{{ lookup('file', 'files/'+ item['username'] + '.key.pub') }}"
La tercera tarea debe contar con el siguiente contenido:
    - name: Add authorized keys
      ansible.posix.authorized_key:
        user: "{{ item['username'] }}"
        key: "{{ lookup('file', 'files/'+ item['username'] + '.key.pub') }}"
      loop: "{{ users }}"
 6.
Agregue una cuarta tarea al play que use el módulo ansible.builtin.lineinfile
para modificar el archivo de configuración sudo y permitir a los miembros del grupo
webadmin usar sudo sin una contraseña en el host gestionado. Use el parámetro
validate para validar la nueva entrada de configuración.
La cuarta tarea debe contar con el siguiente contenido:
    - name: Modify sudo config to allow webadmin users sudo without a password
      ansible.builtin.lineinfile:
        path: /etc/sudoers.d/webadmin
        state: present
        create: true
        mode: 0440
        line: "%webadmin ALL=(ALL) NOPASSWD: ALL"
        validate: /usr/sbin/visudo -cf %s
RH294-RHEL9.0-es-3-20240620
435


capítulo 9 |  Automatización de tareas de administración de Linux
 7.
Agregue una quinta tarea para asegurarse de que el usuario root no tenga permitido
iniciar sesión usando SSH directamente. Use notify: Restart sshd para
desencadenar un manejador para reiniciar SSH.
La quinta tarea debe contar con el siguiente contenido:
    - name: Disable root login via SSH
      ansible.builtin.lineinfile:
        dest: /etc/ssh/sshd_config
        regexp: "^PermitRootLogin"
        line: "PermitRootLogin no"
      notify: Restart sshd
 8.
En la primera línea después de la ubicación del archivo de variables, agregue una nueva
definición de manejador denominado Restart sshd.
8.1.
Defina el manejador Restart sshd de la siguiente manera.
...output omitted...
    - vars/users_vars.yml
  handlers:
    - name: Restart sshd
      ansible.builtin.service:
        name: sshd
        state: restarted
8.2.
El playbook users.yml debería incluir el siguiente contenido:
---
- name: Create multiple local users
  hosts: webservers
  vars_files:
    - vars/users_vars.yml
  handlers:
    - name: Restart sshd
      ansible.builtin.service:
        name: sshd
        state: restarted
  tasks:
    - name: Add webadmin group
      ansible.builtin.group:
        name: webadmin
        state: present
    - name: Create user accounts
      ansible.builtin.user:
        name: "{{ item['username'] }}"
        groups: "{{ item['groups'] }}"
      loop: "{{ users }}"
    - name: Add authorized keys
      ansible.posix.authorized_key:
        user: "{{ item['username'] }}"
436
RH294-RHEL9.0-es-3-20240620


capítulo 9 |  Automatización de tareas de administración de Linux
        key: "{{ lookup('file', 'files/'+ item['username'] + '.key.pub') }}"
      loop: "{{ users }}"
    - name: Modify sudo config to allow webadmin users sudo without a password
      ansible.builtin.lineinfile:
        path: /etc/sudoers.d/webadmin
        state: present
        create: true
        mode: 0440
        line: "%webadmin ALL=(ALL) NOPASSWD: ALL"
        validate: /usr/sbin/visudo -cf %s
    - name: Disable root login via SSH
      ansible.builtin.lineinfile:
        dest: /etc/ssh/sshd_config
        regexp: "^PermitRootLogin"
        line: "PermitRootLogin no"
      notify: Restart sshd
8.3.
Ejecute el playbook users.yml.
[student@workstation system-users]$ ansible-navigator run \
> -m stdout users.yml
PLAY [Create multiple local users] *******************************************
TASK [Gathering Facts] *******************************************************
ok: [servera.lab.example.com]
TASK [Add webadmin group] ****************************************************
ok: [servera.lab.example.com]
TASK [Create user accounts] **************************************************
ok: [servera.lab.example.com] => (item={'username': 'user1', 'groups':
 'webadmin'})
ok: [servera.lab.example.com] => (item={'username': 'user2', 'groups':
 'webadmin'})
ok: [servera.lab.example.com] => (item={'username': 'user3', 'groups':
 'webadmin'})
ok: [servera.lab.example.com] => (item={'username': 'user4', 'groups':
 'webadmin'})
ok: [servera.lab.example.com] => (item={'username': 'user5', 'groups':
 'webadmin'})
TASK [Add authorized keys] ***************************************************
changed: [servera.lab.example.com] => (item={'username': 'user1', 'groups':
 'webadmin'})
changed: [servera.lab.example.com] => (item={'username': 'user2', 'groups':
 'webadmin'})
changed: [servera.lab.example.com] => (item={'username': 'user3', 'groups':
 'webadmin'})
changed: [servera.lab.example.com] => (item={'username': 'user4', 'groups':
 'webadmin'})
RH294-RHEL9.0-es-3-20240620
437


capítulo 9 |  Automatización de tareas de administración de Linux
changed: [servera.lab.example.com] => (item={'username': 'user5', 'groups':
 'webadmin'})
TASK [Modify sudo config to allow webadmin users sudo without a password] ***
changed: [servera.lab.example.com]
TASK [Disable root login via SSH] *******************************************
changed: [servera.lab.example.com]
RUNNING HANDLER [Restart sshd] **********************************************
changed: [servera.lab.example.com]
PLAY RECAP ******************************************************************
servera.lab.example.com    : ok=7    changed=4    unreachable=0    failed=0   
 skipped=0    rescued=0    ignored=0
 9.
Use SSH como usuario user1 e inicie sesión en el servidor servera. Después de iniciar
sesión, use el comando sudo -i para cambiar al usuario root.
9.1.
Use SSH como usuario user1 e inicie sesión en el servidor servera.
[student@workstation system-users]$ ssh user1@servera
Register this system with Red Hat Insights: insights-client --register
Create an account or view all your systems at https://red.ht/insights-dashboard
[user1@servera ~]$
9.2.
Cambie al usuario root.
[user1@servera ~]$ sudo -i
[root@servera ~]#
9.3.
Cierre sesión en servera.
[root@servera ~]# exit
logout
[user1@servera ~]$ exit
logout
Connection to servera closed.
[student@workstation system-users]$
 10. Intente iniciar sesión en servera con el usuario root directamente. Este paso debería
fallar, ya que la configuración del daemon SSH se modificó para no permitir inicios de
sesión directos del usuario root.
10.1.
Desde la máquina workstation, use SSH como usuario root e inicie sesión en el
servidor servera.
[student@workstation system-users]$ ssh root@servera
root@servera's password: redhat
Permission denied, please try again.
root@servera's password:
438
RH294-RHEL9.0-es-3-20240620


capítulo 9 |  Automatización de tareas de administración de Linux
Esto confirma que la configuración SSH denegó el acceso directo al sistema para el
usuario root.
Finalizar
En la máquina workstation, cambie al directorio de inicio de usuario student y use el comando
lab para completar este ejercicio. Este paso es importante para garantizar que los recursos de
ejercicios anteriores no impacten en los siguientes.
[student@workstation ~]$ lab finish system-users
RH294-RHEL9.0-es-3-20240620
439


capítulo 9 |  Automatización de tareas de administración de Linux
Administración del proceso de arranque y
procesos programados
Objetivos
• Administrar el inicio del servicio, programar procesos con at, cron y systemd, reiniciar los
hosts gestionados con reboot y controlar el destino de inicio predeterminado en los hosts
gestionados.
Programación de trabajos para la futura ejecución
Red Hat Enterprise Linux proporciona varios mecanismos que puede usar para programar
comandos a fin de que se ejecuten en algún momento en el futuro. El comando at programa
trabajos que se ejecutan una vez en un momento específico. El subsistema Cron programa
trabajos para que se ejecuten en un programa recurrente, ya sea en el archivo personal crontab
de un usuario, en la configuración Cron del sistema en /etc/crontab o como un archivo en
/etc/cron.d. El subsistema systemd también proporciona unidades de temporizador que
pueden iniciar unidades de servicio en un horario establecido.
Programación de trabajos que se ejecutan una sola vez
La programación rápida y de una vez se realiza con el módulo ansible.posix.at. Se crea la
tarea para que se ejecute en una hora futura y se la retiene hasta el momento de ejecutarse.
Opciones para el módulo ansible.posix.at
Opción
Comentarios
command
El comando para programar la ejecución en el futuro.
count
El número entero de unidades a partir de ahora que se debe
ejecutar el trabajo. (Debe usarse con units).
units
Especifica si count se mide en minutos, horas, días o
semanas.
script_file
Un archivo de script existente programado para que se ejecute
en el futuro.
state
El valor predeterminado (present) agrega un trabajo;
absent elimina un trabajo coincidente si está presente.
unique
Si se establece en true, si un trabajo coincidente ya está
presente, no se agrega un nuevo trabajo.
En el siguiente ejemplo, la tarea que se muestra usa at para programar el comando userdel -r
tempuser para que se ejecute en 20 minutos.
440
RH294-RHEL9.0-es-3-20240620


capítulo 9 |  Automatización de tareas de administración de Linux
- name: Remove tempuser
  ansible.posix.at:
    command: userdel -r tempuser
    count: 20
    units: minutes
    unique: true
Programación de trabajos repetidos con Cron
Puede configurar un comando que se ejecute en una programación repetida mediante Cron.
Para configurar trabajos de Cron, use el módulo ansible.builtin.cron. La opción name es
obligatoria y se inserta en crontab como una descripción del trabajo repetido. El módulo también
lo usa para determinar si el trabajo Cron ya existe o qué trabajo Cron se debe modificar o eliminar.
Algunos parámetros de uso común del módulo ansible.builtin.cron incluyen:
Opciones para el módulo ansible.builtin.cron
Opciones
Comentarios
name
El comentario que identifica el trabajo de Cron.
job
El comando que se ejecutará.
minute, hour, day, month,
weekday
El valor del campo en la especificación de tiempo para el
trabajo en la entrada crontab. Si no se establece, se asume
"*" (todos los valores).
state
Si se establece en present, crea el trabajo Cron (el
predeterminado); absent lo elimina.
user
El trabajo Cron se ejecuta como este usuario. Si no se
especifica cron_file, el trabajo se establece en el archivo
crontab de ese usuario.
cron_file
Si está configurado, cree un trabajo Cron del sistema en
cron_file. Debe especificar user y una especificación de
tiempo. Si usa una ruta relativa, el archivo se crea en /etc/
cron.d.
Esta primera tarea de ejemplo crea un trabajo Cron en el archivo personal crontab del usuario
testing. Ejecuta su script personal backup-home-dir a las 16:00 todos los viernes. Puede
iniciar sesión como ese usuario y ejecutar crontab -l después de ejecutar el playbook para
confirmar que funcionó.
- name: Schedule backups for my home directory
  ansible.builtin.cron:
    name: Backup my home directory
    user: testing
    job: /home/testing/bin/backup-home-dir
    minute: 0
    hour: 16
    weekday: 5
RH294-RHEL9.0-es-3-20240620
441


capítulo 9 |  Automatización de tareas de administración de Linux
En el siguiente ejemplo, la tarea crea un trabajo Cron del sistema en el archivo /etc/cron.d/
flush_bolt que ejecuta un comando como root para vaciar la caché de Bolt cada mañana a las
11:45.
- name: Schedule job to flush the Bolt cache
  ansible.builtin.cron:
    name: Flush Bolt cache
    cron_file: flush_bolt
    user: "root"
    minute: 45
    hour: 11
    job: "php ./app/nut cache:clear"
Advertencia
No use cron_file para modificar el archivo /etc/crontab. El archivo que
especifique solo debe mantenerse por Ansible y solo debe contener la entrada
especificada por la tarea.
Control de las unidades de temporizador de Systemd
El módulo ansible.builtin.systemd se puede usar para habilitar o deshabilitar las unidades
de temporizador systemd existentes que ejecutan trabajos recurrentes (generalmente unidades
de servicio systemd que finalmente salen).
En el siguiente ejemplo, se deshabilita y se detiene el temporizador systemd que completa
automáticamente la caché de paquetes dnf en Red Hat Enterprise Linux 9.
- name: Disable dnf makecache
  ansible.builtin.systemd:
    name: dnf-makecache.timer
    state: stopped
    enabled: false
Gestión de servicios
Puede elegir entre dos módulos para administrar servicios o recargar daemons:
ansible.builtin.systemd y ansible.builtin.service.
El módulo ansible.builtin.service está diseñado para funcionar con varios sistemas
de gestión de servicios, incluidos systemd, Upstart, SysVinit, init BSD y otros. Dado que
proporciona una interfaz genérica para el sistema de inicialización, ofrece un conjunto básico de
opciones para iniciar, detener, reiniciar y habilitar servicios y otros daemons.
- name: Start and enable nginx
  ansible.builtin.service:
    name: nginx
    state: started
    enabled: true
442
RH294-RHEL9.0-es-3-20240620


capítulo 9 |  Automatización de tareas de administración de Linux
El módulo ansible.builtin.systemd está diseñado para funcionar solo con systemd, pero
ofrece opciones de configuración adicionales específicas para ese sistema y administrador de
servicios.
El siguiente ejemplo que usa ansible.builtin.systemd es equivalente al ejemplo anterior
que usa ansible.builtin.service:
- name: Start nginx
  ansible.builtin.systemd:
    name: nginx
    state: started
    enabled: true
En el siguiente ejemplo se recarga el daemon httpd, pero antes de que lo haga, se ejecuta
systemctl daemon-reload para recargar la configuración systemd completa.
- name: Reload web server
  ansible.builtin.systemd:
    name: httpd
    state: reloaded
    daemon_reload: true
Configuración de un objetivo de arranque
predeterminado
El módulo ansible.builtin.systemd no puede establecer el objetivo de arranque
predeterminado. Puede usar el módulo ansible.builtin.command para establecer el objetivo
de arranque predeterminado.
- name: Change default systemd target
  hosts: all
  gather_facts: false
  vars:
    systemd_target: "multi-user.target" 
  tasks:
    - name: Get current systemd target
      ansible.builtin.command:
        cmd: systemctl get-default 
      changed_when: false 
      register: target 
    - name: Set default systemd target
      ansible.builtin.command:
        cmd: systemctl set-default {{ systemd_target }} 
      when: systemd_target not in target['stdout'] 
      become: true 
Esta variable contiene el nombre del objetivo predeterminado que desea.
Esto obtiene el objetivo actual.
RH294-RHEL9.0-es-3-20240620
443


capítulo 9 |  Automatización de tareas de administración de Linux
Debido a que solo se trata de recopilar información, la tarea nunca debe informar changed.
La variable target contiene la información que se recopiló.
Este comando establece el objetivo predeterminado.
Omita esta tarea si el objetivo predeterminado actual ya es el destino predeterminado
deseado. Esto asegura que la tarea sea idempotente.
Esta es la única tarea en este play que requiere acceso root.
Reinicio de hosts gestionados.
Puede usar el módulo dedicado ansible.builtin.reboot para reiniciar los hosts gestionados
durante la ejecución del playbook. Este módulo reinicia el host gestionado y espera hasta que el
host gestionado vuelva a funcionar antes de continuar con la ejecución del playbook. El módulo
determina que un host gestionado está respaldado esperando hasta que Ansible pueda ejecutar
un comando en el host gestionado.
En el siguiente ejemplo simple se desencadena de inmediato un reinicio:
- name: Reboot now
  ansible.builtin.reboot:
De forma predeterminada, el playbook espera hasta 600 segundos antes de decidir que el reinicio
falló y otros 600 segundos antes de decidir que el comando de prueba falló. Puede ajustar este
valor para que los tiempos de espera sean cada 180 segundos. Por ejemplo:
- name: Reboot, shorten timeout
  ansible.builtin.reboot:
    reboot_timeout: 180
Algunas otras opciones útiles para el módulo incluyen:
Opciones para el módulo ansible.builtin.reboot
Opciones
Comentarios
pre_reboot_delay
El tiempo de espera antes de reiniciar. En Linux, esto se mide
en minutos y, si es inferior a 60, se redondea a 0.
msg
El mensaje que se mostrará a los usuarios antes de reiniciar.
test_command
El comando que se usa para determinar si el host gestionado
se puede usar y está listo para más tareas de Ansible después
del reinicio. El valor predeterminado es whoami.
444
RH294-RHEL9.0-es-3-20240620


capítulo 9 |  Automatización de tareas de administración de Linux
Referencias
Módulo ansible.posix.at: programar la ejecución de un comando o archivo de
script mediante el comando at: documentación de Ansible
https://docs.ansible.com/ansible/latest/collections/ansible/posix/at_module.html
Módulo ansible.builtin.cron: gestionar las entradas cron.d y crontab:
documentación de Ansible
https://docs.ansible.com/ansible/latest/collections/ansible/builtin/
cron_module.html
Módulo ansible.builtin.reboot: reiniciar una máquina: documentación de
Ansible
https://docs.ansible.com/ansible/latest/collections/ansible/builtin/
reboot_module.html
Módulo ansible.builtin.service module: gestión de servicios: documentación de
Ansible
https://docs.ansible.com/ansible/latest/modules/service_module.html
Módulo ansible.builtin.systemd: gestión de unidades systemd: documentación
de Ansible
https://docs.ansible.com/ansible/latest/collections/ansible/builtin/
systemd_module.html
RH294-RHEL9.0-es-3-20240620
445


capítulo 9 |  Automatización de tareas de administración de Linux
Ejercicio Guiado
Administración del proceso de arranque y
procesos programados
Gestionar el proceso de inicio, programar trabajos recurrentes y reiniciar los hosts
gestionados.
Resultados
• Programar un trabajo Cron.
• Eliminar un solo trabajo específico de Cron de un archivo crontab.
• Programar una tarea de at.
• Establecer el destino de arranque predeterminado en los hosts gestionados.
• Reiniciar los hosts gestionados.
Antes De Comenzar
Con el usuario student en la máquina workstation, use el comando lab para preparar su
sistema para este ejercicio.
Este comando prepara su entorno y garantiza que estén disponibles todos los recursos
requeridos, incluido el directorio del proyecto /home/student/system-process.
[student@workstation ~]$ lab start system-process
Instrucciones
 1.
Cambie al directorio /home/student/system-process.
[student@workstation ~]$ cd ~/system-process
[student@workstation system-process]$
 2.
Cree el playbook create_crontab_file.yml en el directorio de trabajo.
Configure el playbook para usar el módulo ansible.builtin.cron para crear un
archivo crontab denominado /etc/cron.d/add-date-time que programa un trabajo
cron recurrente. El trabajo debe ejecutarse con el usuario devops cada dos minutos
comenzando a las 09:00 y terminando a las 16:59 de lunes a viernes. El trabajo debe
adjuntar la fecha y hora del archivo /home/devops/my_datetime_cron_job.
2.1.
Cree el playbook create_crontab_file.yml y agregue las líneas necesarias
para iniciar el play. Debe apuntar a los hosts gestionados del grupo webservers y
habilitar el aumento de privilegios.
446
RH294-RHEL9.0-es-3-20240620


capítulo 9 |  Automatización de tareas de administración de Linux
---
- name: Recurring cron job
  hosts: webservers
  become: true
2.2.
Defina una tarea que use el módulo ansible.builtin.cron para
programar un trabajo cron recurrente, el comando date >> /home/devops/
my_date_time_cron_job.
nota
El módulo ansible.builtin.cron proporciona una opción name para describir
de forma única la entrada del archivo crontab y garantizar los resultados
esperados.
El valor que usa para la opción name se agrega al archivo crontab como
comentario. Por ejemplo, se requiere la opción name si elimina una entrada de
crontab usando state: absent.
Además, la opción name evita que se cree siempre una nueva entrada de crontab
cuando se configura el estado predeterminado, state: present.
  tasks:
    - name: Crontab file exists
      ansible.builtin.cron:
        name: Add date and time to a file
        job: date >> /home/devops/my_date_time_cron_job
2.3.
Configure el trabajo para que se ejecute cada dos minutos comenzando a las 09:00 y
finalizando a las 16:59 de lunes a viernes.
        minute: "*/2"
        hour: 9-16
        weekday: 1-5
2.4.
Use el parámetro cron_file para usar el archivo crontab denominado /etc/
cron.d/add-date-time en lugar del crontab de un usuario individual en /var/
spool/cron/.
Una ruta relativa colocará el archivo en el directorio /etc/cron.d.
Si se usa el parámetro cron_file, también debe especificar el parámetro user para
el archivo del sistema crontab. Use el usuario devops para este trabajo.
        user: devops
        cron_file: add-date-time
        state: present
2.5.
Cuando se complete, el playbook debe verse de la siguiente forma. Revise el
playbook para comprobar la precisión.
RH294-RHEL9.0-es-3-20240620
447


capítulo 9 |  Automatización de tareas de administración de Linux
---
- name: Recurring cron job
  hosts: webservers
  become: true
  tasks:
    - name: Crontab file exists
      ansible.builtin.cron:
        name: Add date and time to a file
        job: date >> /home/devops/my_date_time_cron_job
        minute: "*/2"
        hour: 9-16
        weekday: 1-5
        user: devops
        cron_file: add-date-time
        state: present
2.6.
Ejecute el comando ansible-navigator run --syntax-check para verificar la
sintaxis del playbook. Corrija los errores antes de pasar al siguiente paso.
[student@workstation system-process]$ ansible-navigator run \
> -m stdout create_crontab_file.yml --syntax-check
playbook: /home/student/system-process/create_crontab_file.yml
2.7.
Ejecute el playbook create_crontab_file.yml.
[student@workstation system-process]$ ansible-navigator run \
> -m stdout create_crontab_file.yml
PLAY [Recurring cron job] *************************************************
TASK [Gathering Facts] ****************************************************
ok: [servera.lab.example.com]
TASK [Crontab file exists] ************************************************
changed: [servera.lab.example.com]
PLAY RECAP ****************************************************************
servera.lab.example.com    : ok=2    changed=1    unreachable=0    failed=0   
 skipped=0    rescued=0    ignored=0
2.8.
Ejecute el siguiente comando para verificar que el archivo cron /etc/cron.d/add-
date-time existe y su contenido es correcto.
[student@workstation system-process]$ ssh devops@servera \
> "cat /etc/cron.d/add-date-time"
#Ansible: Add date and time to a file
*/2 9-16 * * 1-5 devops date >> /home/devops/my_date_time_cron_job
448
RH294-RHEL9.0-es-3-20240620


capítulo 9 |  Automatización de tareas de administración de Linux
 3.
Cree el playbook remove_cron_job.yml en el directorio de trabajo. Configure el
playbook para usar el módulo ansible.builtin.cron para eliminar el trabajo cron Add
date and time to a file del archivo /etc/cron.d/add-date-time crontab.
3.1.
Cree el playbook remove_cron_job.yml y agregue las siguientes líneas:
---
- name: Remove scheduled cron job
  hosts: webservers
  become: true
  tasks:
    - name: Cron job removed
      ansible.builtin.cron:
        name: Add date and time to a file
        user: devops
        cron_file: add-date-time
        state: absent
3.2.
Ejecute el comando ansible-navigator run --syntax-check para verificar la
sintaxis del playbook. Corrija los errores antes de pasar al siguiente paso.
[student@workstation system-process]$ ansible-navigator run \
> -m stdout remove_cron_job.yml --syntax-check
playbook: /home/student/system-process/remove_cron_job.yml
3.3.
Ejecute el playbook remove_cron_job.yml.
[student@workstation system-process]$ ansible-navigator run \
> -m stdout remove_cron_job.yml
PLAY [Remove scheduled cron job] ******************************************
TASK [Gathering Facts] ****************************************************
ok: [servera.lab.example.com]
TASK [Cron job removed] ***************************************************
changed: [servera.lab.example.com]
PLAY RECAP ****************************************************************
servera.lab.example.com    : ok=2    changed=1    unreachable=0    failed=0   
 skipped=0    rescued=0    ignored=0
3.4.
Ejecute el siguiente comando para verificar que se haya eliminado el archivo /etc/
cron.d/add-date-time.
[student@workstation system-process]$ ssh devops@servera \
> "ls -l /etc/cron.d"
total 4
-rw-r--r--. 1 root root 128 Aug  9  2021 0hourly
RH294-RHEL9.0-es-3-20240620
449


capítulo 9 |  Automatización de tareas de administración de Linux
 4.
Cree el playbook schedule_at_task.yml en el directorio de trabajo. Configure el
playbook para usar el módulo ansible.posix.at para programar una tarea que se
ejecute un minuto en el futuro.
La tarea debe ejecutar el comando date y redirigir la salida al archivo /home/devops/
my_at_date_time.
Use la opción unique: true para asegurarse de que, si el comando ya existe en la cola
at, no se agregue una nueva tarea.
4.1.
Cree el playbook schedule_at_task.yml y agregue las siguientes líneas:
---
- name: Schedule at task
  hosts: webservers
  become: true
  become_user: devops
  tasks:
    - name: Create date and time file
      ansible.posix.at:
        command: date > ~/my_at_date_time
        count: 1
        units: minutes
        unique: true
        state: present
4.2.
Ejecute el comando ansible-navigator run --syntax-check para verificar la
sintaxis del playbook. Corrija los errores antes de pasar al siguiente paso.
[student@workstation system-process]$ ansible-navigator run \
> -m stdout schedule_at_task.yml --syntax-check
playbook: /home/student/system-process/schedule_at_task.yml
4.3.
Ejecute el playbook schedule_at_task.yml.
[student@workstation system-process]$ ansible-navigator run \
> -m stdout schedule_at_task.yml
PLAY [Schedule at task] ***************************************************
TASK [Gathering Facts] ****************************************************
ok: [servera.lab.example.com]
TASK [Create date and time file] ******************************************
changed: [servera.lab.example.com]
PLAY RECAP ****************************************************************
servera.lab.example.com    : ok=2    changed=1    unreachable=0    failed=0   
 skipped=0    rescued=0    ignored=0
4.4.
Después de esperar un minuto hasta que se complete el comando at, ejecute
los siguientes comandos para verificar que el archivo /home/devops/
my_at_date_time existe y tiene el contenido correcto.
450
RH294-RHEL9.0-es-3-20240620


capítulo 9 |  Automatización de tareas de administración de Linux
[student@workstation system-process]$ ssh devops@servera \
> "ls -l my_at_date_time"
-rw-rw-r--. 1 devops devops 32 Aug 15 00:00 my_at_date_time
[student@workstation system-process]$ ssh devops@servera \
> "cat my_at_date_time"
Mon Aug 15 12:00:00 AM EDT 2022
 5.
Cree el playbook set_default_boot_target_graphical.yml en el directorio
de trabajo. Escriba un play en el playbook para establecer el objetivo predeterminado
systemd en graphical.target.
5.1.
Cree el playbook set_default_boot_target_graphical.yml y agregue las
siguientes líneas:
---
- name: Change default boot target
  hosts: webservers
  become: true
  gather_facts: false
  vars:
    default_target: "graphical.target"
  tasks:
    - name: Get current boot target
      ansible.builtin.command:
        cmd: systemctl get-default
      changed_when: false
      register: target
    - name: Set default boot target
      ansible.builtin.command:
        cmd: systemctl set-default {{ default_target }}
      when: default_target not in target['stdout']
5.2.
Ejecute el comando ansible-navigator run --syntax-check para verificar la
sintaxis del playbook. Corrija los errores antes de pasar al siguiente paso.
[student@workstation system-process]$ ansible-navigator run \
> -m stdout set_default_boot_target_graphical.yml --syntax-check
playbook: /home/student/system-process/set_default_boot_target_graphical.yml
5.3.
Antes de ejecutar el playbook, ejecute el siguiente comando para verificar que el
objetivo de arranque predeterminado actual sea multi-user.target.
[student@workstation system-process]$ ssh devops@servera \
> "systemctl get-default"
multi-user.target
5.4.
Ejecute el playbook set_default_boot_target_graphical.yml.
RH294-RHEL9.0-es-3-20240620
451


capítulo 9 |  Automatización de tareas de administración de Linux
[student@workstation system-process]$ ansible-navigator run \
> -m stdout set_default_boot_target_graphical.yml
PLAY [Change default boot target] *****************************************
TASK [Get current boot target] ********************************************
ok: [servera.lab.example.com]
TASK [Set default boot target] ********************************************
changed: [servera.lab.example.com]
PLAY RECAP ****************************************************************
servera.lab.example.com    : ok=2    changed=1    unreachable=0    failed=0   
 skipped=0    rescued=0    ignored=0
5.5.
Ejecute el siguiente comando para verificar que el destino de arranque
predeterminado ahora sea graphical.target.
[student@workstation system-process]$ ssh devops@servera \
> "systemctl get-default"
graphical.target
 6.
Cree el playbook reboot_hosts.yml en el directorio de trabajo para reiniciar los hosts
gestionados.
6.1.
Cree el playbook reboot_hosts.yml y agregue las siguientes líneas:
---
- name: Reboot hosts
  hosts: webservers
  become: true
  tasks:
    - name: Hosts are rebooted
      ansible.builtin.reboot:
6.2.
Ejecute el comando ansible-navigator run --syntax-check para verificar la
sintaxis del playbook. Corrija los errores antes de pasar al siguiente paso.
[student@workstation system-process]$ ansible-navigator run \
> -m stdout reboot_hosts.yml --syntax-check
playbook: /home/student/system-process/reboot_hosts.yml
6.3.
Antes de ejecutar el playbook, ejecute el siguiente comando para determinar el sello
de hora del último reinicio del sistema:
[student@workstation system-process]$ ssh devops@servera \
> "who -b"
         system boot  2022-08-15 00:07
6.4.
Ejecute el playbook reboot_hosts.yml.
452
RH294-RHEL9.0-es-3-20240620


capítulo 9 |  Automatización de tareas de administración de Linux
[student@workstation system-process]$ ansible-navigator run \
> -m stdout reboot_hosts.yml
PLAY [Reboot hosts] *******************************************************
TASK [Gathering Facts] ****************************************************
ok: [servera.lab.example.com]
TASK [Hosts are rebooted] *************************************************
changed: [servera.lab.example.com]
PLAY RECAP ****************************************************************
servera.lab.example.com    : ok=2    changed=1    unreachable=0    failed=0   
 skipped=0    rescued=0    ignored=0
6.5.
Ejecute el siguiente comando para determinar el sello de hora del último arranque del
sistema. El sello de hora que se muestra después de que se ejecuta el playbook debe
ser posterior.
[student@workstation system-process]$ ssh devops@servera \
> "who -b"
         system boot  2022-08-15 00:44
6.6.
Ejecute el siguiente comando para determinar que el objetivo de arranque
graphical.target sigue siendo el predeterminado después del reinicio.
[student@workstation system-process]$ ssh devops@servera \
> "systemctl get-default"
graphical.target
 7.
Para mantener la coherencia a lo largo de los ejercicios restantes, revierta el destino de
arranque predeterminado a la configuración anterior, multi-user.target.
Copie su playbook set_default_boot_target_graphical.yml en
set_default_boot_target_multi-user.yml en el directorio de proyectos de
Ansible.
Edite la variable default_target para configurar el archivo multi-user.target como
predeterminado.
7.1.
Copie su playbook set_default_boot_target_graphical.yml a
set_default_boot_target_multi-user.yml.
[student@workstation system-process]$ cp set_default_boot_target_graphical.yml \
> set_default_boot_target_multi-user.yml
7.2.
Edite su play en el playbook set_default_boot_target_multi-user.yml para
cambiar la variable default_target a multi-user.target.
---
- name: Change default boot target
  hosts: webservers
RH294-RHEL9.0-es-3-20240620
453


capítulo 9 |  Automatización de tareas de administración de Linux
  become: true
  gather_facts: false
  vars:
    default_target: "multi-user.target"
  tasks:
    - name: Get current boot target
      ansible.builtin.command:
        cmd: systemctl get-default
      changed_when: false
      register: target
    - name: Set default boot target
      ansible.builtin.command:
        cmd: systemctl set-default {{ default_target }}
      when: default_target not in target['stdout']
7.3.
Ejecute el comando ansible-navigator run --syntax-check para verificar la
sintaxis del playbook. Corrija los errores antes de pasar al siguiente paso.
[student@workstation system-process]$ ansible-navigator run \
> -m stdout set_default_boot_target_multi-user.yml --syntax-check
playbook: /home/student/system-process/set_default_boot_target_multi-user.yml
7.4.
Ejecute el playbook set_default_boot_target_multi-user.yml.
[student@workstation system-process]$ ansible-navigator run \
> -m stdout set_default_boot_target_multi-user.yml
PLAY [Change default boot target] *****************************************
TASK [Get current boot target] ********************************************
ok: [servera.lab.example.com]
TASK [Set default boot target] ***********************************
changed: [servera.lab.example.com]
PLAY RECAP ****************************************************************
servera.lab.example.com    : ok=2    changed=1    unreachable=0    failed=0   
 skipped=0    rescued=0    ignored=0
7.5.
Ejecute el siguiente comando para verificar que el destino de arranque
predeterminado ahora sea multi-user.target.
[student@workstation system-process]$ ssh devops@servera \
> "systemctl get-default"
multi-user.target
Finalizar
En la máquina workstation, cambie al directorio de inicio de usuario student y use el comando
lab para completar este ejercicio. Este paso es importante para garantizar que los recursos de
ejercicios anteriores no impacten en los siguientes.
454
RH294-RHEL9.0-es-3-20240620


capítulo 9 |  Automatización de tareas de administración de Linux
[student@workstation ~]$ lab finish system-process
RH294-RHEL9.0-es-3-20240620
455


capítulo 9 |  Automatización de tareas de administración de Linux
Administración del almacenamiento
Objetivos
• Particionar los dispositivos de almacenamiento, configurar LVM, formatear particiones o
volúmenes lógicos, montar sistemas de archivos y agregar espacios de intercambio (swap).
Montaje de sistemas de archivos existentes
Use el módulo ansible.posix.mount para montar un sistema de archivos existente. Los
parámetros más comunes para el módulo ansible.posix.mount son el parámetro path,
que especifica la ruta para montar el sistema de archivos, el parámetro src, que especifica
el dispositivo (esto podría ser un nombre de dispositivo, UUID o volumen NFS), el parámetro
fstype, que especifica el tipo de sistema de archivos, y el parámetro state, que acepta los
valores absent, mounted, present, unmounted o remounted.
La siguiente tarea de ejemplo monta el recurso compartido de NFS disponible en
172.25.250.100:/share en el directorio /nfsshare de los hosts gestionados.
- name: Mount NFS share
  ansible.posix.mount:
    path: /nfsshare
    src: 172.25.250.100:/share
    fstype: nfs
    opts: defaults
    dump: '0'
    passno: '0'
    state: mounted
Configuración de almacenamiento con el rol de
sistema de almacenamiento
Red Hat Ansible Automation Platform proporciona el rol del sistema
redhat.rhel_system_roles.storage para configurar dispositivos de almacenamiento
local en sus hosts gestionados. Puede gestionar sistemas de archivos en dispositivos de bloques
sin particiones y formatear y crear volúmenes lógicos en volúmenes físicos LVM basados en
dispositivos de bloques sin particiones.
Importante
El rol redhat.rhel_system_roles.storage soporta formalmente la gestión de
sistemas de archivos y las entradas de montaje para dos casos de uso:
• Dispositivos sin particiones (sistemas de archivos de dispositivos completos)
• LVM en volúmenes físicos de dispositivos completos sin particiones
Si tiene otros casos de uso, es posible que deba usar otros módulos y roles para
implementarlos.
456
RH294-RHEL9.0-es-3-20240620


capítulo 9 |  Automatización de tareas de administración de Linux
Administración de un sistema de archivos en un dispositivo sin
particiones
Para crear un sistema de archivos en un dispositivo de bloques sin particiones con el rol
redhat.rhel_system_roles.storage, defina la variable storage_volumes. La variable
storage_volumes contiene una lista de dispositivos de almacenamiento para gestionar.
Los siguientes ítems del diccionario están disponibles en la variable storage_volumes:
Parámetros de la variable storage_volumes
Parámetro
Comentarios
name
El nombre del volumen.
type
Este valor debe ser disk.
disks
Debe ser una lista de exactamente un ítem; el dispositivo de
bloque sin particiones.
mount_point
El directorio en el que está montado el sistema de archivos.
fstype
El tipo de sistema de archivos que se usará. (xfs, ext4 o
swap.)
mount_options
Opciones de montaje personalizadas, como ro o rw.
El siguiente play de ejemplo crea un sistema de archivos XFS en el dispositivo /dev/vdg y lo
monta en /opt/extra.
- name: Example of a simple storage device
  hosts: all
  roles:
    - name: redhat.rhel_system_roles.storage
      storage_volumes:
        - name: extra
          type: disk
          disks:
            - /dev/vdg
          fs_type: xfs
          mount_point: /opt/extra
Gestión de LVM con el rol de almacenamiento
Para crear un grupo de volúmenes LVM con el rol redhat.rhel_system_roles.storage,
defina la variable storage_pools. La variable storage_pools contiene una lista de conjuntos
(pools) (grupos de volúmenes LVM) para gestionar.
Los ítems del diccionario dentro de la variable storage_pools se usan de la siguiente manera:
• La variable name es el nombre del grupo de volúmenes.
• La variable type debe tener el valor lvm.
• La variable disks es la lista de dispositivos de bloque que el grupo de volúmenes usa para su
almacenamiento.
RH294-RHEL9.0-es-3-20240620
457


capítulo 9 |  Automatización de tareas de administración de Linux
• La variable volumes es la lista de volúmenes lógicos en el grupo de volúmenes.
La siguiente entrada crea el grupo de volumen vg01 con la clave type establecida en el valor lvm.
El volumen físico del grupo de volúmenes es el disco /dev/vdb.
---
- name: Configure storage on webservers
  hosts: webservers
  roles:
    - name: redhat.rhel_system_roles.storage
      storage_pools:
        - name: vg01
          type: lvm
          disks:
            - /dev/vdb
Importante
La opción disks solo soporta dispositivos de bloques sin particiones para sus
volúmenes físicos LVM.
Para crear volúmenes lógicos, complete la variable volumes, anidada bajo la variable
storage_pools, con una lista de nombres de volúmenes lógicos y sus parámetros. Cada
ítem de la lista es un diccionario que representa un único volumen lógico dentro de la variable
storage_pools.
Cada ítem de la lista de volúmenes lógicos tiene las siguientes variables de diccionario:
• name: el nombre del volumen lógico.
• size: el tamaño del volumen lógico.
• mount_point: el directorio usado como punto de montaje para el sistema de archivos del
volumen lógico.
• fs_type: el tipo de sistema de archivos del volumen lógico.
• state: si el volumen lógico debe existir usando los valores present o absent.
En el siguiente ejemplo se crean dos volúmenes lógicos, denominados lvol01 y lvol02. El
volumen lógico lvol01 tiene un tamaño de 128 MB, está formateado con el sistema de archivos
xfs y está montado en /data. El volumen lógico lvol02 tiene un tamaño de 256 MB, está
formateado con el sistema de archivos xfs y está montado en /backup.
---
- name: Configure storage on webservers
  hosts: webservers
  roles:
    - name: redhat.rhel_system_roles.storage
      storage_pools:
        - name: vg01
          type: lvm
          disks:
            - /dev/vdb
          volumes:
            - name: lvol01
458
RH294-RHEL9.0-es-3-20240620


capítulo 9 |  Automatización de tareas de administración de Linux
              size: 128m
              mount_point: "/data"
              fs_type: xfs
              state: present
            - name: lvol02
              size: 256m
              mount_point: "/backup"
              fs_type: xfs
              state: present
En la siguiente entrada de ejemplo, si el volumen lógico lvol01 ya se creó con un tamaño de
128 MB, el volumen lógico y el sistema de archivos se amplían a 256 MB, suponiendo que el
espacio está disponible dentro del grupo de volúmenes.
          volumes:
            - name: lvol01
              size: 256m
              mount_point: "/data"
              fs_type: xfs
              state: present
Configuración del espacio de intercambio (swap)
Puede usar el rol redhat.rhel_system_roles.storage para crear volúmenes lógicos
formateados como espacios de intercambio (swap). El rol crea el volumen lógico, el tipo de
sistema de archivos de intercambio (swap), agrega el volumen de intercambio al archivo /etc/
fstab y habilita el volumen de intercambio de inmediato.
En el siguiente ejemplo del playbook se crea el volumen lógico lvswap en el grupo de volúmenes
vgswap, se agrega el volumen de intercambio (swap) al archivo /etc/fstab y se habilita el
espacio de intercambio.
---
- name: Configure a swap volume
  hosts: all
  roles:
    - name: redhat.rhel_system_roles.storage
      storage_pools:
        - name: vgswap
          type: lvm
          disks:
            - /dev/vdb
          volumes:
            - name: lvswap
              size: 512m
              fs_type: swap
              state: present
RH294-RHEL9.0-es-3-20240620
459


capítulo 9 |  Automatización de tareas de administración de Linux
Gestión de particiones y sistemas de archivos con
tareas
Puede gestionar particiones y sistemas de archivos en sus dispositivos de almacenamiento sin
usar el rol del sistema. Sin embargo, los módulos más convenientes para hacer esto actualmente
no cuentan con el soporte de Red Hat, lo que puede hacer que esto sea más complicado.
Advertencia
Tenga cuidado cuando use Ansible para particionar y formatear sistemas de
archivos, especialmente si usa tareas ansible.builtin.command. Los errores en
su código en sistemas importantes pueden resultar en la pérdida de datos.
Gestión de particiones
Si desea particionar sus dispositivos de almacenamiento sin usar el rol del sistema, sus opciones
son un poco más complejas.
• El módulo community.general.parted no soportado en Ansible Content Collection
community.general puede realizar esta tarea.
• Puede usar el módulo ansible.builtin.command para ejecutar los comandos de partición
en los hosts gestionados. Sin embargo, debe tener especial cuidado para asegurarse de que los
comandos sean idempotentes y no destruyan inadvertidamente los datos en sus dispositivos de
almacenamiento existentes.
Por ejemplo, la siguiente tarea crea una etiqueta de disco GPT y una partición [ /dev/sda1 en el
dispositivo de almacenamiento /dev/sda solo si /dev/sda1 aún no existe:
- name: Ensure that /dev/sda1 exists
  ansible.builtin.command:
    cmd: parted --script mklabel gpt mkpart primary 1MiB 100%
    creates: /dev/sda1
Esto depende del hecho de que, si la partición /dev/sda1 existe, un sistema Linux crea un
archivo de dispositivo /dev/sda1 para ella automáticamente.
Administración de sistemas de archivos
La forma más sencilla de gestionar sistemas de archivos sin usar el rol del sistema podría ser el
módulo community.general.filesystem. Sin embargo, Red Hat no admite este módulo, por
lo que lo usa bajo su propia responsabilidad.
Como alternativa, puede usar el módulo ansible.builtin.command para ejecutar comandos
para formatear sistemas de archivos. Sin embargo, debe usar algún mecanismo para asegurarse
de que el dispositivo que está formateando no contenga ya un sistema de archivos, para garantizar
la idempotencia de su play y para evitar la pérdida accidental de datos. Una forma de hacerlo
podría ser revisar los datos relacionados con el almacenamiento recopilados por Ansible para
determinar si un dispositivo parece estar formateado con un sistema de archivos.
460
RH294-RHEL9.0-es-3-20240620


capítulo 9 |  Automatización de tareas de administración de Linux
Datos de Ansible para la configuración de
almacenamiento
Los datos de Ansible recopilados por ansible.builtin.setup contienen información útil
sobre los dispositivos de almacenamiento en sus hosts gestionados.
Datos sobre los dispositivos de bloque
El dato ansible_facts['devices'] incluye información de todos los dispositivos de
almacenamiento disponibles en el host gestionado. Esto incluye información adicional como las
particiones en cada dispositivo o el tamaño total de cada dispositivo.
El siguiente playbook recopila y muestra el dato ansible_facts['devices'] para cada host
gestionado.
---
- name: Display storage facts
  hosts: all
  tasks:
    - name: Display device facts
      ansible.builtin.debug:
        var: ansible_facts['devices']
Este dato contiene un diccionario de variables nombradas para los dispositivos en el sistema. Cada
variable de dispositivo nombrada tiene un diccionario de variables para su valor, que representa
información sobre el dispositivo. Por ejemplo, si tiene el dispositivo /dev/sda en su sistema,
puede usar la siguiente expresión Jinja2 (todo en una línea) para determinar su tamaño en bytes:
{{ ansible_facts['devices']['sda']['sectors'] * ansible_facts['devices']['sda']
['sectorsize'] }}
Datos seleccionados de un diccionario de variables de dispositivo
Dato
Comentarios
host
Una cadena que identifica el controlador al que está
conectado el dispositivo de bloque.
model
Una cadena que identifica el modelo del dispositivo de
almacenamiento, si corresponde.
partitions
Un diccionario de dispositivos de bloque que son particiones
en este dispositivo. Cada variable de diccionario tiene
como valor un diccionario estructurado como cualquier otro
dispositivo (incluidos los valores para sectors, size, etc.).
sectors
La cantidad de sectores de almacenamiento que contiene el
dispositivo.
sectorsize
El tamaño de cada sector en bytes.
RH294-RHEL9.0-es-3-20240620
461


capítulo 9 |  Automatización de tareas de administración de Linux
Dato
Comentarios
size
Un cálculo aproximado legible por humanos del tamaño del
dispositivo.
Por ejemplo, puede encontrar el tamaño de /dev/sda1 a partir del siguiente dato:
ansible_facts['devices']['sda']['partitions']['sda1']['size']
Datos sobre los enlaces de dispositivos
El dato ansible_facts['device_links'] incluye todos los vínculos disponibles para
cada dispositivo de almacenamiento. Si tiene dispositivos de múltiples rutas, puede usar
esto para ayudar a determinar qué dispositivos son rutas alternativas al mismo dispositivo de
almacenamiento o son dispositivos de múltiples rutas.
El siguiente playbook recopila y muestra el dato ansible_['device_links'] para todos los
hosts gestionados.
---
- name: Gather device link facts
  hosts: all
  tasks:
    - name: Display device link facts
      ansible.builtin.debug:
        var: ansible_facts['device_links']
Datos sobre los sistemas de archivos montados
El dato ansible_facts['mounts'] proporciona información sobre los dispositivos montados
actualmente en el host gestionado. Para cada dispositivo, esto incluye el dispositivo de bloque
montado, el punto de montaje de su sistema de archivos, las opciones de montaje, etc.
El siguiente playbook recopila y muestra el dato ansible_facts['mounts'] para los hosts
gestionados.
---
- name: Gather mounts
  hosts: all
  tasks:
    - name: Display mounts facts
      ansible.builtin.debug:
        var: ansible_facts['mounts']
El dato contiene una lista de diccionarios para cada sistema de archivos montado en el host
gestionado.
462
RH294-RHEL9.0-es-3-20240620


capítulo 9 |  Automatización de tareas de administración de Linux
Variables seleccionadas del diccionario en un ítem de lista del sistema de archivos montado
Variable
Comentarios
mount
El directorio en el que está montado el sistema de archivos.
device
El nombre del dispositivo de bloque que está montado.
fstype
El tipo de sistema de archivos con el que está formateado el
dispositivo (como xfs).
options
Las opciones de montaje actuales en vigor.
size_total
El tamaño total del dispositivo.
size_available
Cuánto espacio hay libre en el dispositivo.
block_size
El tamaño de los bloques en el sistema de archivos.
block_total
Cuántos bloques hay en el sistema de archivos.
block_available
Cuántos bloques hay en el sistema de archivos.
inode_available
Cuántos inodos hay libres en el sistema de archivos.
Por ejemplo, puede determinar el espacio libre en el sistema de archivos root (/) en cada host
gestionado con el siguiente play:
- name: Print free space on / file system
  hosts: all
  gather_facts: true 
  tasks:
    - name: Display free space
      ansible.builtin.debug:
        msg: >
          The root file system on {{ ansible_facts['fqdn'] }} has
          {{ item['block_available'] * item['block_size'] / 1000000 }}
          megabytes free. 
      loop: "{{ ansible_facts['mounts'] }}" 
      when: item['mount'] == '/' 
Reúna los datos automáticamente.
La matemática dentro de la segunda expresión Jinja2 calcula megabytes decimales de
espacio libre.
El bucle itera sobre cada sistema de archivos montado en la lista.
Los condicionales se verifican en cada iteración del bucle. El bucle se desenrolla, pero la
tarea solo ejecuta el módulo cuando el condicional coincide.
RH294-RHEL9.0-es-3-20240620
463


capítulo 9 |  Automatización de tareas de administración de Linux
Referencias
mount: controlar puntos de montaje activos y configurados (documentación
de Ansible)
https://docs.ansible.com/ansible/latest/collections/ansible/posix/
mount_module.html
Roles: Documentación de Ansible
https://docs.ansible.com/ansible/latest/user_guide/playbooks_reuse_roles.html
Managing local storage using RHEL System Roles — Red Hat Documentation
https://access.redhat.com/documentation/en-us/red_hat_enterprise_linux/9/html/
administration_and_configuration_tasks_using_system_roles_in_rhel/managing-
local-storage-using-rhel-system-roles_assembly_updating-packages-to-enable-
automation-for-the-rhel-system-roles
464
RH294-RHEL9.0-es-3-20240620


capítulo 9 |  Automatización de tareas de administración de Linux
Ejercicio Guiado
Administración del almacenamiento
Asignar un nuevo disco como un volumen físico LVM para un grupo de volúmenes, crea
volúmenes lógicos, los formatea con sistemas de archivos XFS y los monta de forma
inmediata y automática en el momento del arranque en sus hosts gestionados.
Resultados
• Usar el rol redhat.rhel_system_roles.storage para gestionar volúmenes y grupos
de volúmenes de LVM.
• Usar el rol redhat.rhel_system_roles.storage para crear sistemas de archivos.
• Usar el rol redhat.rhel_system_roles.storage para controlar y configurar puntos
de montaje en /etc/fstab.
Antes De Comenzar
Con el usuario student en la máquina workstation, use el comando lab para preparar su
sistema para este ejercicio.
Este comando prepara su entorno y garantiza que estén disponibles todos los recursos
requeridos.
[student@workstation ~]$ lab start system-storage
Instrucciones
Usted es responsable de administrar un conjunto de servidores web. Debe configurarlos
para almacenar los datos del sitio web en un sistema de archivos separado de los archivos de
configuración y de registro para los servidores web. Esta es una práctica recomendada.
Escriba un playbook para lo siguiente:
• Use el dispositivo /dev/vdb como volumen físico LVM, contribuyendo con espacio al grupo de
volúmenes apache-vg.
• Cree dos volúmenes lógicos denominados content-lv (de 64 ) y logs-lv (de 128 MB),
ambos respaldados por el grupo de volúmenes apache-vg.
• Crear un sistema de archivos XFS en ambos volúmenes lógicos.
• Monte el volumen lógico content-lv en el directorio /var/www.
• Monte el volumen lógico logs-lv en el directorio /var/log/httpd.
 1.
Cambie al directorio del proyecto /home/student/system-storage.
[student@workstation ~]$ cd ~/system-storage
[student@workstation system-storage]$
RH294-RHEL9.0-es-3-20240620
465


capítulo 9 |  Automatización de tareas de administración de Linux
 2.
Instale la colección rhel_system_roles desde el archivo redhat-
rhel_system_roles-1.19.3.tar.gz en el directorio collections del directorio del
proyecto.
2.1.
Use el comando ansible-galaxy para instalar la colección rhel_system_roles
desde el archivo redhat-rhel_system_roles-1.19.3.tar.gz en el directorio
collections.
[student@workstation system-storage]$ ansible-galaxy collection install \
> ./redhat-rhel_system_roles-1.19.3.tar.gz -p collections
...output omitted...
redhat.rhel_system_roles:1.19.3 was installed successfully
 3.
Cree el playbook storage.yml. Escriba un play en ese playbook que ejecute el rol del
sistema redhat.rhel_system_roles.storage en los hosts gestionados en el grupo
webservers. Ese play debe configurar el volumen físico de LVM, el grupo de volúmenes y
los volúmenes lógicos para los servidores web.
3.1.
Cree el playbook storage.yml dirigida al grupo webservers y aplica el rol
redhat.rhel_system_roles.storage.
---
- name: Configure storage on webservers
  hosts: webservers
  roles:
    - name: redhat.rhel_system_roles.storage
3.2.
Defina la variable de rol storage_pools para el rol
redhat.rhel_system_roles.storage. Úselo para establecer el nombre del
grupo de volúmenes en apache-vg, el tipo en lvm y especificar que los discos usen
el dispositivo /dev/vdb.
      storage_pools:
        - name: apache-vg
          type: lvm
          disks:
            - /dev/vdb
3.3.
Defina la variable volumes en la variable del rol storage_pools.
          volumes:
3.4.
Cree un volumen lógico dentro de volumes con el nombre content-lv, un tamaño
de 64 MB, un tipo de sistema de archivos de xfs y un punto de montaje de /var/
www.
            - name: content-lv
              size: 64m
              mount_point: "/var/www"
              fs_type: xfs
              state: present
466
RH294-RHEL9.0-es-3-20240620


capítulo 9 |  Automatización de tareas de administración de Linux
3.5.
Cree otro volumen lógico dentro de volumes con el nombre logs-lv, un tamaño de
128 MB, un tipo de sistema de archivos de xfs y un punto de montaje de /var/log/
httpd.
            - name: logs-lv
              size: 128m
              mount_point: "/var/log/httpd"
              fs_type: xfs
              state: present
El playbook final debería incluir el siguiente contenido:
---
- name: Configure storage on webservers
  hosts: webservers
  roles:
    - name: redhat.rhel_system_roles.storage
      storage_pools:
        - name: apache-vg
          type: lvm
          disks:
            - /dev/vdb
          volumes:
            - name: content-lv
              size: 64m
              mount_point: "/var/www"
              fs_type: xfs
              state: present
            - name: logs-lv
              size: 128m
              mount_point: "/var/log/httpd"
              fs_type: xfs
              state: present
 4.
Ejecute el playbook storage.yml.
[student@workstation system-storage]$ ansible-navigator run \
> -m stdout storage.yml
PLAY [Configure storage on webservers] *****************************************
TASK [Gathering Facts] *********************************************************
ok: [servera.lab.example.com]
...output omitted...
TASK [rhel-system-roles.storage : make sure blivet is available] ***************
changed: [servera.lab.example.com]
...output omitted...
TASK [rhel-system-roles.storage : set up new/current mounts] *******************
RH294-RHEL9.0-es-3-20240620
467


capítulo 9 |  Automatización de tareas de administración de Linux
changed: [servera.lab.example.com] => (item={'src': '/dev/mapper/apache--vg-
content--lv', 'path': '/var/www', 'fstype': 'xfs', 'opts': 'defaults', 'dump': 0,
 'passno': 0, 'state': 'mounted'})
changed: [servera.lab.example.com] => (item={'src': '/dev/mapper/apache--vg-logs--
lv', 'path': '/var/log/httpd', 'fstype': 'xfs', 'opts': 'defaults', 'dump': 0,
 'passno': 0, 'state': 'mounted'})
...output omitted...
PLAY RECAP *********************************************************************
servera.lab.example.com    : ok=21   changed=3    unreachable=0    failed=0   
 skipped=12   rescued=0    ignored=0
 5.
Ejecute el playbook get-storage.yml proporcionado en el directorio del proyecto
para verificar que el almacenamiento se haya configurado correctamente en los hosts
gestionados en el grupo webservers. Si es así, la información sobre el almacenamiento
aparece en la salida del playbook como se resalta en el siguiente ejemplo:
[student@workstation system-storage]$ ansible-navigator run \
> -m stdout get-storage.yml
PLAY [View storage configuration] **********************************************
TASK [Gathering Facts] *********************************************************
ok: [servera.lab.example.com]
TASK [Retrieve physical volumes] ***********************************************
changed: [servera.lab.example.com]
TASK [Display physical volumes] ************************************************
ok: [servera.lab.example.com] => {
    "msg": [
        "  PV         VG        Fmt  Attr PSize    PFree  ",
        "  /dev/vdb   apache-vg lvm2 a--  1020.00m 828.00m"
    ]
}
TASK [Retrieve volume groups] **************************************************
changed: [servera.lab.example.com]
TASK [Display volume groups] ***************************************************
ok: [servera.lab.example.com] => {
    "msg": [
        "  VG        #PV #LV #SN Attr   VSize    VFree  ",
        "  apache-vg   1   2   0 wz--n- 1020.00m 828.00m"
    ]
}
TASK [Retrieve logical volumes] ************************************************
changed: [servera.lab.example.com]
TASK [Display logical volumes] *************************************************
ok: [servera.lab.example.com] => {
    "msg": [
468
RH294-RHEL9.0-es-3-20240620


capítulo 9 |  Automatización de tareas de administración de Linux
        "  LV         VG        Attr       LSize   Pool Origin Data%  Meta%  Move
 Log Cpy%Sync Convert",
        "  content-lv apache-vg -wi-ao----  64.00m                                
                    ",
        "  logs-lv    apache-vg -wi-ao---- 128.00m                                
                    "
    ]
}
TASK [Retrieve mounted logical volumes] ****************************************
changed: [servera.lab.example.com]
TASK [Display mounted logical volumes] *****************************************
ok: [servera.lab.example.com] => {
    "msg": [
        "/dev/mapper/apache--vg-content--lv on /var/www type xfs
 (rw,relatime,seclabel,attr2,inode64,logbufs=8,logbsize=32k,noquota)",
        "/dev/mapper/apache--vg-logs--lv on /var/log/httpd type xfs
 (rw,relatime,seclabel,attr2,inode64,logbufs=8,logbsize=32k,noquota)"
    ]
}
TASK [Retrieve /etc/fstab contents] ********************************************
changed: [servera.lab.example.com]
TASK [Display /etc/fstab contents] *********************************************
ok: [servera.lab.example.com] => {
    "msg": [
        "UUID=5e75a2b9-1367-4cc8-bb38-4d6abc3964b8\t/boot\txfs\tdefaults\t0\t0",
        "UUID=fb535add-9799-4a27-b8bc-e8259f39a767\t/\txfs\tdefaults\t0\t0",
        "UUID=7B77-95E7\t/boot/efi\tvfat
\tdefaults,uid=0,gid=0,umask=077,shortname=winnt\t0\t2",
        "/dev/mapper/apache--vg-content--lv /var/www xfs defaults 0 0",
        "/dev/mapper/apache--vg-logs--lv /var/log/httpd xfs defaults 0 0"
    ]
}
PLAY RECAP *********************************************************************
servera.lab.example.com    : ok=11   changed=5    unreachable=0    failed=0   
 skipped=0    rescued=0    ignored=0
Finalizar
En la máquina workstation, cambie al directorio de inicio de usuario student y use el comando
lab para completar este ejercicio. Este paso es importante para garantizar que los recursos de
ejercicios anteriores no impacten en los siguientes.
[student@workstation ~]$ lab finish system-storage
RH294-RHEL9.0-es-3-20240620
469


capítulo 9 |  Automatización de tareas de administración de Linux
Administración de la configuración de red
Objetivos
• Configurar los ajustes de red y la resolución de nombres en los hosts gestionados y recopilar
datos de Ansible relacionados con la red.
Configuración de redes con el rol de sistema de red
El rol del sistema redhat.rhel_system_roles.network proporciona una manera de
automatizar la configuración de las interfaces de red y las configuraciones relacionadas con la red
en los hosts gestionados de Red Hat Enterprise Linux.
Este rol soporta la configuración de interfaces Ethernet, interfaces de puente, interfaces
enlazadas, interfaces VLAN, interfaces MACVLAN e interfaces Infiniband e interfaces
inalámbricas.
El rol se configura mediante el uso de dos variables: network_provider y
network_connections.
---
network_provider: nm
network_connections:
  - name: ens4
    type: ethernet
    ip:
      address:
        - 172.25.250.30/24
La variable network_provider configura el proveedor de back end, ya sea nm
(NetworkManager) o initscripts. En Red Hat Enterprise Linux 7 y versiones posteriores, use
nm como proveedor de red predeterminado.
Si aún tiene sistemas Red Hat Enterprise Linux 6 en Extended Lifecycle Support (ELS), debe usar
el proveedor initscripts para esos hosts gestionados. Ese proveedor requiere que el servicio
heredado network esté disponible en los hosts gestionados.
La variable network_connections configura las diferentes conexiones. Toma como valor
una lista de diccionarios, cada uno de los cuales representa configuraciones para una conexión
específica. Use el nombre de la interfaz como nombre de la conexión.
En la siguiente tabla, se enumeran las opciones para la variable network_connections.
470
RH294-RHEL9.0-es-3-20240620


capítulo 9 |  Automatización de tareas de administración de Linux
Opciones seleccionadas para la variable network_connections
Nombre de la opción
Descripción
name
Para NetworkManager, identifica el perfil de conexión (la
opción connection.id ). Para initscripts, identifica el
nombre del archivo de configuración (/etc/sysconfig/
network-scripts/ifcfg-name).
state
El estado de tiempo de ejecución de un perfil de conexión.
Puede ser up, si el perfil de conexión está activo, o down, si no
lo está.
persistent_state
Identifica si un perfil de conexión es persistente. Puede ser
present si el perfil de conexión es persistente, o absent si
no lo es.
type
Identifica el tipo de conexión. Los valores válidos son
ethernet, bridge, bond, team, vlan, macvlan,
infiniband y wireless.
autoconnect
Determina si la conexión se inicia automáticamente.
Establecer en yes de forma predeterminada.
mac
Restringe la conexión que se usará en dispositivos con esta
dirección MAC específica.
interface_name
Restringe el perfil de conexión que usará una interfaz
específica.
zone
Configura la zona firewalld para la interfaz.
ip
Determina la configuración de IP para la conexión. Soporta las
opciones address para especificar una lista de direcciones
IPv4 o IPv6 estáticas en la interfaz, gateway4 o gateway6
para especificar el enrutador predeterminado IPv4 o IPv6 y
dns para configurar una lista de servidores DNS.
La variable ip a su vez toma un diccionario de variables para su configuración. No es necesario
usar todos estos elementos. Una conexión puede tener una configuración address con una sola
dirección IPv4, o puede omitir la configuración de la dirección y tener dhcp4: yes configurado
para habilitar el direccionamiento DHCPv4.
Opciones seleccionadas para la variable ip
Nombre de la opción
Descripción
address
Una lista de direcciones IPv4 o IPv6 estáticas y prefijos de
máscara de red para la conexión.
gateway4
Establece una dirección estática del enrutador IPv4
predeterminado.
gateway6
Establece una dirección estática del enrutador IPv6
predeterminado.
RH294-RHEL9.0-es-3-20240620
471


capítulo 9 |  Automatización de tareas de administración de Linux
Nombre de la opción
Descripción
dns
Una lista de servidores de nombres DNS para la conexión.
dhcp4
Use DHCPv4 para configurar la interfaz.
auto6
Use la configuración automática de IPv6 para configurar la
interfaz.
Esta es una variable mínima de ejemplo network_connections para configurar y activar
inmediatamente una dirección IPv4 estática para la interfaz enp1s0:
network_connections:
- name: enp1s0
  type: ethernet
  ip:
    address:
      - 192.0.2.25/24
    state: up
Si estaba configurando dinámicamente la interfaz usando DHCP y SLAAC, podría usar la siguiente
configuración en su lugar:
network_connections:
- name: enp1s0
  type: ethernet
  ip:
    dhcp4: true
    auto6: true
  state: up
En el siguiente ejemplo se desactiva temporalmente una interfaz de red existente:
network_connections:
- name: enp1s0
  type: ethernet
  state: down
Para eliminar la configuración de enp1s0 por completo, debe escribir la variable de la siguiente
manera:
network_connections:
- name: enp1s0
  type: ethernet
  state: down
  persistent_state: absent
En el siguiente ejemplo, se usan algunas de estas opciones para configurar la interfaz eth0 con
una dirección IPv4 estática, establecer un servidor de nombres DNS estático y colocar la interfaz
en la zona external para firewalld:
472
RH294-RHEL9.0-es-3-20240620


capítulo 9 |  Automatización de tareas de administración de Linux
network_connections:
- name: eth0 
    persistent_state: present  
    type: ethernet  
    autoconnect: yes  
    mac: 00:00:5e:00:53:5d  
    ip:
      address:
        - 172.25.250.40/24  
      dns:
        - 8.8.8.8 
    zone: external  
Use eth0 como nombre de conexión.
Haga que la conexión sea persistente. Este es el valor predeterminado.
Establezca el tipo de partición en ethernet.
Inicie automáticamente la conexión durante el arranque. Este es el valor predeterminado.
Restrinja la configuración de la conexión para que la use solo un dispositivo con esa dirección
MAC.
Configure la dirección IP 172.25.250.40/24 para la conexión.
Configure 8.8.8.8 como el servidor de nombres DNS para la conexión.
Configure la zona external como la zona firewalld de la conexión.
En el siguiente play de ejemplo, se establece network_connections como una variable de play
y luego invoca al rol redhat.rhel_system_roles.network:
- name: NIC Configuration
  hosts: webservers
  vars:
    network_connections:
      - name: ens4
        type: ethernet
        ip:
          address:
            - 172.25.250.30/24
  roles:
    - redhat.rhel_system_roles.network
Puede especificar variables para el rol de red con la cláusula vars, como en el ejemplo anterior,
como variables de rol. Alternativamente, puede crear un archivo YAML con esas variables en los
directorios group_vars o host_vars, según su caso de uso.
Puede usar este rol para configurar conexiones inalámbricas 802.11, VLAN, puentes y otras
configuraciones de red más complejas. Consulte la documentación del rol para obtener más
detalles y ejemplos.
RH294-RHEL9.0-es-3-20240620
473


capítulo 9 |  Automatización de tareas de administración de Linux
Configuración de redes con módulos
Además del rol del sistema redhat.rhel_system_roles.network, Ansible incluye módulos
que soportan la configuración del nombre de host y el firewall en un sistema.
El módulo ansible.builtin.hostname establece el nombre de host para un host gestionado
sin modificar el archivo /etc/hosts. Este módulo usa el parámetro name para especificar el
nuevo nombre de host, como en la siguiente tarea:
- name: Change hostname
  ansible.builtin.hostname:
    name: managedhost1
El módulo ansible.posix.firewalld soporta la gestión de firewalld en hosts
gestionados.
Este módulo soporta la configuración de las reglas firewalld para servicios y puertos. También
soporta la gestión de zona, incluida la asociación de interfaces y reglas de red a una zona
específica.
La siguiente tarea muestra cómo crear una regla de firewalld para el servicio http en la zona
predeterminada (public). La siguiente tarea configura la regla como permanente y se asegura de
que esté activa:
- name: Enabling http rule
  ansible.posix.firewalld:
    service: http
    permanent: true
    state: enabled
Esta siguiente tarea configura el eth0 en la zona external firewalld:
- name: Moving eth0 to external
  ansible.posix.firewalld:
    zone: external
    interface: eth0
    permanent: true
    state: enabled
La siguiente tabla enumera algunos de los parámetros para el módulo
ansible.posix.firewalld.
Nombre del parámetro
Descripción
interface
El nombre de la interfaz que se gestionará con
firewalld.
port
Puerto o rango de puertos. Usa el formato
puerto/protocolo o puerto-puerto/protocolo.
rich_rule
Regla enriquecida para firewalld.
service
El nombre del servicio que se gestionará con
firewalld.
474
RH294-RHEL9.0-es-3-20240620


capítulo 9 |  Automatización de tareas de administración de Linux
Nombre del parámetro
Descripción
source
Red de origen que se gestionará con
firewalld.
zone
Zona de firewalld.
state
Habilita o deshabilita una configuración de
firewalld.
type
Tipo de dispositivo o conexión de red.
permanent
El cambio persiste en los reinicios.
immediate
Si los cambios se establecen en permanent,
aplíquelos inmediatamente.
Datos de Ansible para la configuración de red
Ansible recopila una serie de datos relacionados con la configuración de red de cada host
gestionado. Por ejemplo, una lista de las interfaces de red en un host gestionado está disponible
en el dato ansible_facts['interfaces'].
El siguiente playbook recopila y muestra las interfaces disponibles para un host:
---
- name: Obtain interface facts
  hosts: host.lab.example.com
  tasks:
    - name: Display interface facts
      ansible.builtin.debug:
        var: ansible_facts['interfaces']
El playbook anterior produce la siguiente lista de interfaces de red:
PLAY [Obtain interface facts] **************************************************
TASK [Gathering Facts] *********************************************************
ok: [host.lab.example.com]
TASK [Display interface facts] *************************************************
ok: [host.lab.example.com] => {
    "ansible_facts['interfaces']": [
        "eth2",
        "eth1",
        "eth0",
        "lo"
    ]
}
RH294-RHEL9.0-es-3-20240620
475


capítulo 9 |  Automatización de tareas de administración de Linux
PLAY RECAP *********************************************************************
host.lab.example.com    : ok=2    changed=0    unreachable=0    failed=0   
 skipped=0    rescued=0    ignored=0
La salida del ejemplo anterior muestra que hay cuatro interfaces de red disponibles en el host
gestionado host.lab.example.com: lo, eth2, eth1 y eth0.
Puede recuperar información adicional sobre la configuración de una interfaz de red
específica a partir del dato ansible_facts['NIC_name']. Por ejemplo, el siguiente
play muestra la configuración de la interfaz de red eth0 imprimiendo el valor del dato
ansible_facts['eth0'].
- name: Obtain eth0 facts
  hosts: host.lab.example.com
  tasks:
    - name: Display eth0 facts
      ansible.builtin.debug:
        var: ansible_facts['eth0']
El playbook anterior produce la siguiente salida:
PLAY [Obtain eth0 facts] *******************************************************
TASK [Gathering Facts] *********************************************************
ok: [host.lab.example.com]
TASK [Display eth0 facts] ******************************************************
ok: [host.lab.example.com] => {
    "ansible_facts['eth0']": {
        "active": true,
        "device": "eth0",
        "features": {
...output omitted...
        },
        "hw_timestamp_filters": [],
        "ipv4": {
            "address": "172.25.250.10",
            "broadcast": "172.25.250.255",
            "netmask": "255.255.255.0",
            "network": "172.25.250.0",
            "prefix": "24"
        },
        "ipv6": [
            {
                "address": "fe80::82a0:2335:d88a:d08f",
                "prefix": "64",
                "scope": "link"
            }
        ],
        "macaddress": "52:54:00:00:fa:0a",
        "module": "virtio_net",
        "mtu": 1500,
476
RH294-RHEL9.0-es-3-20240620


capítulo 9 |  Automatización de tareas de administración de Linux
        "pciid": "virtio0",
        "promisc": false,
        "speed": -1,
        "timestamping": [],
        "type": "ether"
    }
}
PLAY RECAP *********************************************************************
host.lab.example.com    : ok=2    changed=0    unreachable=0    failed=0   
 skipped=0    rescued=0    ignored=0
En la salida anterior se muestran detalles de configuración adicionales, como la configuración de
dirección IP para IPv4 e IPv6, el dispositivo asociado y el tipo de interfaz.
Importante
Los diferentes hosts gestionados pueden tener diferentes nombres de interfaz de
red, según su configuración de hardware y software. Los datos de Ansible pueden
ayudarlo a identificar las diferencias entre los sistemas para que pueda abordarlas o
mitigarlas en su automatización.
La siguiente tabla enumera algunos otros datos útiles relacionados con la red.
Nombre del dato
Descripción
ansible_facts['dns']
Una lista de las direcciones IP del servidor de
nombres DNS y los dominios de búsqueda.
ansible_facts['domain']
El subdominio para el host gestionado.
ansible_facts['all_ipv4_addresses'] Todas las direcciones IPv4 configuradas en el
host gestionado.
ansible_facts['all_ipv6_addresses'] Todas las direcciones IPv6 configuradas en el
host gestionado.
ansible_facts['fqdn']
El nombre de dominio completamente
calificado (FQDN) del host gestionado.
ansible_facts['hostname']
El nombre de host no calificado (la parte del
nombre de host antes del primer punto en el
FQDN).
ansible_facts['nodename']
El nombre de host para el host gestionado
según lo informado por el sistema.
nota
Ansible también proporciona la "variable mágica" inventory_hostname que
incluye el nombre de host como está configurado en el archivo de inventario de
Ansible.
RH294-RHEL9.0-es-3-20240620
477


capítulo 9 |  Automatización de tareas de administración de Linux
Referencias
Knowledgebase: Red Hat Enterprise Linux (RHEL) System Roles
https://access.redhat.com/articles/3050101
Roles de sistema de Linux
https://linux-system-roles.github.io/
linux-system-roles/network en GitHub
https://github.com/linux-system-roles/network
Documentación de módulo ansible.builtin.hostname
https://docs.ansible.com/ansible/latest/collections/ansible/builtin/
hostname_module.html
Documentación de módulo ansible.posix.firewalld
https://docs.ansible.com/ansible/latest/collections/ansible/posix/
firewalld_module.html
478
RH294-RHEL9.0-es-3-20240620


capítulo 9 |  Automatización de tareas de administración de Linux
Ejercicio Guiado
Administración de la configuración de red
Ajustar la configuración de red de un host gestionado y recopilar información sobre él en un
archivo creado por una plantilla.
Resultados
• Deberá ser capaz de configurar los ajustes de red y en los hosts gestionados y recopilar
datos de Ansible relacionados con la red.
Antes De Comenzar
Con el usuario student en la máquina workstation, use el comando lab para preparar su
sistema para este ejercicio.
Este comando prepara su entorno y garantiza que estén disponibles todos los recursos
requeridos.
[student@workstation ~]$ lab start system-network
Instrucciones
 1.
Revise el archivo de inventario del directorio del proyecto /home/student/system-
network.
1.1.
Cambie al directorio /home/student/system-network.
[student@workstation ~]$ cd ~/system-network
[student@workstation system-network]$
1.2.
En el archivo inventory, verifique que servera.lab.example.com sea parte del
grupo de hosts webservers. Este servidor tiene una interfaz de red de repuesto.
[student@workstation system-network]$ cat inventory
[webservers]
servera.lab.example.com
 2.
Instale la Ansible Content Collection redhat.rhel_system_roles desde el archivo
redhat-rhel_system_roles-1.19.3.tar.gz en el directorio collections del
directorio del proyecto.
2.1.
Use el comando ansible-galaxy para instalar la Ansible Content
Collection rhel_system_roles desde el archivo redhat-
rhel_system_roles-1.19.3.tar.gz en el directorio collections del
proyecto.
RH294-RHEL9.0-es-3-20240620
479


capítulo 9 |  Automatización de tareas de administración de Linux
[student@workstation system-network]$ ansible-galaxy collection install \
> ./redhat-rhel_system_roles-1.19.3.tar.gz -p collections
Starting galaxy collection install process
Process install dependency map
Starting collection install process
Installing 'redhat.rhel_system_roles:1.19.3' to '/home/student/system-network/
collections/ansible_collections/redhat/rhel_system_roles'
redhat.rhel_system_roles:1.19.3 was installed successfully
 3.
Cree un playbook que use el rol redhat.rhel_system_roles.network para
configurar la interfaz de red eth1 en servera.lab.example.com con la dirección IP
172.25.250.30/24 y el prefijo de red.
3.1.
Cree un playbook denominado playbook.yml y agregue un play orientado al grupo
de hosts webservers. Incluya el rol rhel_system_roles.network en la sección
roles del play.
---
- name: NIC Configuration
  hosts: webservers
  roles:
    - redhat.rhel_system_roles.network
3.2.
La documentación para el rol del sistema redhat.rhel_system_roles.network
enumera las variables y opciones que están disponibles para configurar su
funcionamiento.
Ejecute ansible-navigator collections, seleccione la colección
redhat.rhel_system_roles y, luego, seleccione el rol network para ver su
documentación.
Revise la sección Setting the IP configuration: (cerca de la línea 1219 de la
documentación) y determine qué variable se requiere para configurar la interfaz de
red eth1 con una dirección IP estática y un prefijo de red.
Para su comodidad, esa parte de la documentación dice lo siguiente:
...output omitted...
Setting the IP configuration:
```yaml
network_connections:
  - name: eth0
    type: ethernet
    ip:
      route_metric4: 100
      dhcp4: no
      #dhcp4_send_hostname: no
      gateway4: 192.0.2.1
      dns:
        - 192.0.2.2
480
RH294-RHEL9.0-es-3-20240620


capítulo 9 |  Automatización de tareas de administración de Linux
        - 198.51.100.5
      dns_search:
        - example.com
        - subdomain.example.com
      dns_options:
        - rotate
        - timeout:1
      route_metric6: -1
      auto6: no
      gateway6: 2001:db8::1
      address:
        - 192.0.2.3/24
        - 198.51.100.3/26
        - 2001:db8::80/7
...output omitted...
3.3.
Cree el subdirectorio group_vars/webservers.
[student@workstation system-network]$ mkdir -pv group_vars/webservers
mkdir: created directory 'group_vars'
mkdir: created directory 'group_vars/webservers'
3.4.
Cree un nuevo archivo de variable denominado network.yml en el directorio
group_vars/webservers para definir la variable de rol network_connections
para el grupo webservers.
El valor de esa variable debe configurar una conexión de red para la interfaz de red
eth1 que le asigna la dirección IP estática y el prefijo de red 172.25.250.30/24.
Al completarse, el archivo incluye el siguiente contenido:
[student@workstation system-network]$ cat group_vars/webservers/network.yml
---
network_connections:
  - name: eth1
    type: ethernet
    ip:
      address:
        - 172.25.250.30/24
3.5.
Ejecute el playbook para configurar la interfaz de red secundaria en servera.
[student@workstation system-network]$ ansible-navigator run \
> -m stdout playbook.yml
PLAY [NIC Configuration] *******************************************************
TASK [Gathering Facts] *********************************************************
ok: [servera.lab.example.com]
TASK [redhat.rhel_system_roles.network : Ensure ansible_facts used by role] ****
RH294-RHEL9.0-es-3-20240620
481


capítulo 9 |  Automatización de tareas de administración de Linux
included: /home/student/system-network/collections/ansible_collections/redhat/
rhel_system_roles/roles/network/tasks/set_facts.yml for servera.lab.example.com
TASK [redhat.rhel_system_roles.network : Ensure ansible_facts used by role are
 present] ***
ok: [servera.lab.example.com]
TASK [redhat.rhel_system_roles.network : Check which services are running] *****
ok: [servera.lab.example.com]
TASK [redhat.rhel_system_roles.network : Check which packages are installed] ***
ok: [servera.lab.example.com]
TASK [redhat.rhel_system_roles.network : Print network provider] ***************
ok: [servera.lab.example.com] => {
    "msg": "Using network provider: nm"
}
TASK [redhat.rhel_system_roles.network : Abort applying the network state
 configuration if using the network_state variable with the initscripts provider]
 ***
skipping: [servera.lab.example.com]
...output omitted...
TASK [redhat.rhel_system_roles.network : Configure networking connection profiles]
 ***
changed: [servera.lab.example.com]
TASK [redhat.rhel_system_roles.network : Configure networking state] ***********
skipping: [servera.lab.example.com]
TASK [redhat.rhel_system_roles.network : Show stderr messages for the
 network_connections] ***
ok: [servera.lab.example.com] => {
    "__network_connections_result.stderr_lines": [
        "[002] <info>  #0, state:None persistent_state:present, 'eth1': add
 connection eth1, c0177289-f461-4042-b9d4-86fd1b235be1"
    ]
}
TASK [redhat.rhel_system_roles.network : Show debug messages for the
 network_connections] ***
skipping: [servera.lab.example.com]
TASK [redhat.rhel_system_roles.network : Show debug messages for the
 network_state] ***
skipping: [servera.lab.example.com]
TASK [redhat.rhel_system_roles.network : Re-test connectivity] *****************
482
RH294-RHEL9.0-es-3-20240620


capítulo 9 |  Automatización de tareas de administración de Linux
ok: [servera.lab.example.com]
PLAY RECAP *********************************************************************
servera.lab.example.com    : ok=10   changed=1    unreachable=0    failed=0   
 skipped=12   rescued=0    ignored=0
 4.
Ejecute el playbook get-eth1.yml proporcionado en el directorio del proyecto para
verificar que la configuración de la interfaz de red eth1 en servera sea correcta.
Verifique que la interfaz de red eth1 use la dirección IP 172.25.250.30 con el prefijo
de red /24 (equivalente a la máscara de subred 255.255.255.0). La configuración de la
dirección IP puede demorar hasta un minuto.
[student@workstation system-network]$ ansible-navigator run \
> -m stdout get-eth1.yml
PLAY [Obtain network info for webservers] **************************************
TASK [Gathering Facts] *********************************************************
ok: [servera.lab.example.com]
TASK [Display eth1 info] *******************************************************
ok: [servera.lab.example.com] => {
    "ansible_facts['eth1']['ipv4']": {
        "address": "172.25.250.30",
        "broadcast": "172.25.250.255",
        "netmask": "255.255.255.0",
        "network": "172.25.250.0",
        "prefix": "24"
    }
}
PLAY RECAP *********************************************************************
servera.lab.example.com    : ok=2    changed=0    unreachable=0    failed=0   
 skipped=0    rescued=0    ignored=0
Finalizar
En la máquina workstation, cambie al directorio de inicio de usuario student y use el comando
lab para completar este ejercicio. Este paso es importante para garantizar que los recursos de
ejercicios anteriores no impacten en los siguientes.
[student@workstation ~]$ lab finish system-network
RH294-RHEL9.0-es-3-20240620
483


capítulo 9 |  Automatización de tareas de administración de Linux
Trabajo de laboratorio
Automatización de tareas de
administración de Linux
Realizar tareas administrativas comunes de Linux en sus hosts gestionados mediante
técnicas que se trataron en este capítulo.
Resultados
• Cree playbooks para configurar un repositorio de software, usuarios y grupos, volúmenes
lógicos, trabajos de cron e interfaces de red adicionales en un host gestionado.
Antes De Comenzar
Con el usuario student en la máquina workstation, use el comando lab para preparar su
sistema para este ejercicio.
Este comando prepara su entorno y garantiza que estén disponibles todos los recursos
requeridos.
[student@workstation ~]$ lab start system-review
Instrucciones
1.
Cree un playbook denominado repo_playbook.yml para que se ejecute en el grupo de
hosts webservers que configure esos hosts gestionados para usar el repositorio interno de
Yum ubicado en http://materials.example.com/yum/repository y luego instale el
paquete rhelver disponible desde ese repositorio en esos hosts gestionados.
La configuración del repositorio debe cumplir con los siguientes requisitos:
• La configuración se encuentra en el archivo /etc/yum.repos.d/example.repo.
• El ID del repositorio es example-internal.
• La URL base es http://materials.example.com/yum/repository.
• El repositorio está configurado para verificar las firmas RPM GPG.
• La descripción del repositorio es Example Inc. Internal YUM repo.
Los paquetes RPM están firmados con un par de claves GPG organizativas. Esa clave pública
de GPG está disponible en http://materials.example.com/yum/repository/RPM-
GPG-KEY-example. Debe asegurarse de que la clave esté configurada en todos los hosts
gestionados en el grupo de hosts webservers.
Ejecute el playbook. Debe confirmar que el playbook instaló el paquete en sus hosts
gestionados después de su ejecución.
2.
Cree un playbook denominado users.yml para ejecutar en el grupo de hosts webservers
que crea el grupo de usuarios webadmin, agrega los usuarios ops1 y ops2 y garantiza que
ambos usuarios tengan webadmin como grupo complementario en esos hosts gestionados.
Ejecute el playbook. Debe confirmar que los usuarios existen en los hosts gestionados y que
tienen webadmin como grupo complementario después de que se ejecute el playbook.
484
RH294-RHEL9.0-es-3-20240620


capítulo 9 |  Automatización de tareas de administración de Linux
3.
Instale la Ansible Content Collection redhat-rhel_system_roles-1.19.3.tar.gz
proporcionada en el directorio del proyecto en el directorio collections.
Cree un playbook denominado storage.yml para que se ejecute en el grupo de
hosts webservers y configure esos hosts gestionados mediante el rol del sistema
redhat.rhel_system_roles.storage, de la siguiente manera:
• Use el dispositivo /dev/vdb como volumen físico LVM para el grupo de volúmenes
apache-vg.
• Crea el volumen lógico content-lv, con un tamaño de 64 MB, en el grupo de volúmenes
apache-vg.
• Crea el volumen lógico logs-lv, con un tamaño de 128 MB, en el grupo de volúmenes
apache-vg.
• Formatea cada volumen lógico con un sistema de archivos XFS.
• Monta el volumen lógico content-lv en el directorio /var/www.
• Monta el volumen lógico logs-lv en el directorio /var/log/httpd.
Ejecute el playbook. Confirme que el playbook se ejecute correctamente después de
ejecutarlo.
4.
Configure el playbook denominado create_crontab_file.yml para ejecutar el grupo de
host webservers que usa el módulo ansible.builtin.cron para crear un sistema de
archivos crontab que programa un trabajo cron recurrente. Cree el archivo /etc/cron.d/
disk_usage con el archivo crontab del sistema. Configure el trabajo Cron del sistema de
ese archivo de la siguiente manera:
• Debe ejecutarse con el usuario devops.
• Debe ejecutarse cada dos minutos de 9:00 a 16:59 de lunes a viernes.
• Debe ejecutar el comando df >> /home/devops/disk_usage.
Ejecute el playbook. Debe confirmar que configuró el trabajo de Cron correctamente
después de ejecutar el playbook. Puede ver si el archivo se implementó correctamente o
inspeccionar el archivo de registro /var/log/cron como root para ver si el trabajo se está
ejecutando.
5.
Cree un playbook denominado network_playbook.yml para ejecutar el grupo de host
webservers que use el rol redhat.rhel_system_roles.network para configurar la
interfaz de red eth1 con la dirección IP 172.25.250.40/24.
Ejecute el playbook. Confirme que el playbook se haya ejecutado correctamente.
Evaluación
Con el usuario student en la máquina workstation, use el comando lab para calificar su
trabajo. Corrija los errores informados y vuelva a ejecutar el comando hasta obtener un resultado
satisfactorio.
[student@workstation ~]$ lab grade system-review
Finalizar
En la máquina workstation, cambie al directorio de inicio de usuario student y use el comando
lab para completar este ejercicio. Este paso es importante para garantizar que los recursos de
ejercicios anteriores no impacten en los siguientes.
[student@workstation ~]$ lab finish system-review
RH294-RHEL9.0-es-3-20240620
485


capítulo 9 |  Automatización de tareas de administración de Linux
486
RH294-RHEL9.0-es-3-20240620


capítulo 9 |  Automatización de tareas de administración de Linux
Solución
Automatización de tareas de
administración de Linux
Realizar tareas administrativas comunes de Linux en sus hosts gestionados mediante
técnicas que se trataron en este capítulo.
Resultados
• Cree playbooks para configurar un repositorio de software, usuarios y grupos, volúmenes
lógicos, trabajos de cron e interfaces de red adicionales en un host gestionado.
Antes De Comenzar
Con el usuario student en la máquina workstation, use el comando lab para preparar su
sistema para este ejercicio.
Este comando prepara su entorno y garantiza que estén disponibles todos los recursos
requeridos.
[student@workstation ~]$ lab start system-review
Instrucciones
1.
Cree un playbook denominado repo_playbook.yml para que se ejecute en el grupo de
hosts webservers que configure esos hosts gestionados para usar el repositorio interno de
Yum ubicado en http://materials.example.com/yum/repository y luego instale el
paquete rhelver disponible desde ese repositorio en esos hosts gestionados.
La configuración del repositorio debe cumplir con los siguientes requisitos:
• La configuración se encuentra en el archivo /etc/yum.repos.d/example.repo.
• El ID del repositorio es example-internal.
• La URL base es http://materials.example.com/yum/repository.
• El repositorio está configurado para verificar las firmas RPM GPG.
• La descripción del repositorio es Example Inc. Internal YUM repo.
Los paquetes RPM están firmados con un par de claves GPG organizativas. Esa clave pública
de GPG está disponible en http://materials.example.com/yum/repository/RPM-
GPG-KEY-example. Debe asegurarse de que la clave esté configurada en todos los hosts
gestionados en el grupo de hosts webservers.
Ejecute el playbook. Debe confirmar que el playbook instaló el paquete en sus hosts
gestionados después de su ejecución.
1.1.
Cambie al directorio /home/student/system-review.
[student@workstation ~]$ cd ~/system-review
[student@workstation system-review]$
RH294-RHEL9.0-es-3-20240620
487


capítulo 9 |  Automatización de tareas de administración de Linux
1.2.
Cree el playbook repo_playbook.yml que se ejecuta en los hosts gestionados en el
grupo de hosts webservers.
Agregue una tarea que use el módulo ansible.builtin.yum_repository para
asegurar la configuración correcta del repositorio yum interno en el host remoto.
La configuración debe cumplir con los siguientes requisitos:
• La configuración se almacena en el archivo /etc/yum.repos.d/example.repo.
• El ID del repositorio es example-internal.
• La URL base es http://materials.example.com/yum/repository.
• El repositorio está configurado para verificar las firmas RPM GPG.
• La descripción del repositorio es Example Inc. Internal YUM repo.
El playbook incluye el siguiente contenido:
---
- name: Repository Configuration
  hosts: webservers
  tasks:
    - name: Ensure Example Repo exists
      ansible.builtin.yum_repository:
        name: example-internal
        description: Example Inc. Internal YUM repo
        file: example
        baseurl: http://materials.example.com/yum/repository/
        gpgcheck: true
1.3.
Agregue una segunda tarea al play que use el módulo ansible.builtin.rpm_key
para asegurarse de que la clave pública del repositorio esté presente en el host remoto.
La URL de la clave pública del repositorio es http://materials.example.com/
yum/repository/RPM-GPG-KEY-example.
La segunda tarea incluye el siguiente contenido:
    - name: Ensure Repo RPM Key is Installed
      ansible.builtin.rpm_key:
        key: http://materials.example.com/yum/repository/RPM-GPG-KEY-example
        state: present
1.4.
Agregue una tercera tarea para instalar el paquete rhelver disponible en el
repositorio interno de Yum.
La tercera tarea incluye el siguiente contenido:
    - name: Install rhelver package
      ansible.builtin.dnf:
        name: rhelver
        state: present
1.5.
Ejecute el playbook repo_playbook.yml:
[student@workstation system-review]$ ansible-navigator run \
> -m stdout repo_playbook.yml
PLAY [Repository Configuration] ************************************************
488
RH294-RHEL9.0-es-3-20240620


capítulo 9 |  Automatización de tareas de administración de Linux
TASK [Gathering Facts] *********************************************************
ok: [serverb.lab.example.com]
TASK [Ensure Example Repo exists] **********************************************
changed: [serverb.lab.example.com]
TASK [Ensure Repo RPM Key is Installed] ****************************************
changed: [serverb.lab.example.com]
TASK [Install rhelver package] *************************************************
changed: [serverb.lab.example.com]
PLAY RECAP *********************************************************************
serverb.lab.example.com    : ok=4    changed=3    unreachable=0    failed=0   
 skipped=0    rescued=0    ignored=0
2.
Cree un playbook denominado users.yml para ejecutar en el grupo de hosts webservers
que crea el grupo de usuarios webadmin, agrega los usuarios ops1 y ops2 y garantiza que
ambos usuarios tengan webadmin como grupo complementario en esos hosts gestionados.
Ejecute el playbook. Debe confirmar que los usuarios existen en los hosts gestionados y que
tienen webadmin como grupo complementario después de que se ejecute el playbook.
2.1.
Cree un archivo de variables vars/users_vars.yml, que define a dos usuarios,
ops1 y ops2, que pertenecen al grupo de usuarios webadmin.
Es posible que deba crear el subdirectorio vars.
[student@workstation system-review]$ mkdir vars
[student@workstation system-review]$ vim vars/users_vars.yml
---
users:
  - username: ops1
    groups: webadmin
  - username: ops2
    groups: webadmin
2.2.
Cree el playbook users.yml. Defina un solo play en el playbook que se dirija al grupo
de hosts webservers.
Agregue una cláusula vars_files que defina la ubicación del nombre de archivo
vars/users_vars.yml.
Agregue una tarea que use el módulo group para crear el grupo de usuarios webadmin
en el host remoto.
El playbook incluye el siguiente contenido:
---
- name: Create multiple local users
  hosts: webservers
  vars_files:
    - vars/users_vars.yml
  tasks:
    - name: Add webadmin group
RH294-RHEL9.0-es-3-20240620
489


capítulo 9 |  Automatización de tareas de administración de Linux
      ansible.builtin.group:
        name: webadmin
        state: present
2.3.
Agregue una segunda tarea al playbook que usa el módulo ansible.builtin.user
para crear los usuarios.
Agregue una cláusula loop: "{{ users }}" a la tarea para recorrer en bucle el
archivo de variables para cada nombre de usuario encontrado en el archivo vars/
users_vars.yml.
Use la variable item['username'] para el valor name:. El archivo de variables podría
contener información adicional útil para crear los usuarios, como los grupos a los que
deberían pertenecer.
La segunda tarea incluye el siguiente contenido:
    - name: Create user accounts
      ansible.builtin.user:
        name: "{{ item['username'] }}"
        groups: webadmin
      loop: "{{ users }}"
2.4.
Ejecute el playbook users.yml:
[student@workstation system-review]$ ansible-navigator run \
> -m stdout users.yml
PLAY [Create multiple local users] *********************************************
TASK [Gathering Facts] *********************************************************
ok: [serverb.lab.example.com]
TASK [Add webadmin group] ******************************************************
changed: [serverb.lab.example.com]
TASK [Create user accounts] ****************************************************
changed: [serverb.lab.example.com] => (item={'username': 'ops1', 'groups':
 'webadmin'})
changed: [serverb.lab.example.com] => (item={'username': 'ops2', 'groups':
 'webadmin'})
PLAY RECAP *********************************************************************
serverb.lab.example.com    : ok=3    changed=2    unreachable=0    failed=0   
 skipped=0    rescued=0    ignored=0
3.
Instale la Ansible Content Collection redhat-rhel_system_roles-1.19.3.tar.gz
proporcionada en el directorio del proyecto en el directorio collections.
Cree un playbook denominado storage.yml para que se ejecute en el grupo de
hosts webservers y configure esos hosts gestionados mediante el rol del sistema
redhat.rhel_system_roles.storage, de la siguiente manera:
• Use el dispositivo /dev/vdb como volumen físico LVM para el grupo de volúmenes
apache-vg.
490
RH294-RHEL9.0-es-3-20240620


capítulo 9 |  Automatización de tareas de administración de Linux
• Crea el volumen lógico content-lv, con un tamaño de 64 MB, en el grupo de volúmenes
apache-vg.
• Crea el volumen lógico logs-lv, con un tamaño de 128 MB, en el grupo de volúmenes
apache-vg.
• Formatea cada volumen lógico con un sistema de archivos XFS.
• Monta el volumen lógico content-lv en el directorio /var/www.
• Monta el volumen lógico logs-lv en el directorio /var/log/httpd.
Ejecute el playbook. Confirme que el playbook se ejecute correctamente después de
ejecutarlo.
3.1.
Instale la colección redhat-rhel_system_roles del archivo ~/system-review/
redhat-rhel_system_roles-1.19.3.tar.gz en el directorio ~/system-
review/collections.
[student@workstation system-review]$ ansible-galaxy collection \
> install ./redhat-rhel_system_roles-1.19.3.tar.gz -p collections
3.2.
Cree el subdirectorio group_vars/webservers.
[student@workstation system-review]$ mkdir -pv group_vars/webservers
mkdir: created directory 'group_vars'
mkdir: created directory 'group_vars/webservers'
3.3.
Cree el archivo de variables ~/system-review/group_vars/webservers/
storage_vars.yml.
En el archivo de variables, defina una variable storage_pool con el nombre de
conjunto (pool) apache-vg para el grupo de volúmenes en el dispositivo /dev/vdb y
con el tipo establecido en lvm.
Dentro del conjunto (pool) apache-vg, defina dos volúmenes lógicos:
• Defina el volumen lógico content-lv con un tamaño de 64 MB formateado con el
sistema de archivos XFS, montado en /var/www.
• Defina el volumen lógico logs-lv con un tamaño de 128 MB formateado con el
sistema de archivos XFS, montado en /var/log/httpd.
Al completarse, el archivo de variables ~/system-review/group_vars/
webservers/storage_vars.yml debe incluir el siguiente contenido:
---
storage_pools:
  - name: apache-vg
    type: lvm
    disks:
      - /dev/vdb
    volumes:
      - name: content-lv
        size: 64m
        mount_point: "/var/www"
        fs_type: xfs
        state: present
      - name: logs-lv
        size: 128m
RH294-RHEL9.0-es-3-20240620
491


capítulo 9 |  Automatización de tareas de administración de Linux
        mount_point: "/var/log/httpd"
        fs_type: xfs
        state: present
3.4.
Cree el playbook storage.yml para aplicar el rol
redhat.rhel_system_roles.storage al grupo de hosts webservers.
Al completarse, el playbook debe incluir el siguiente contenido:
---
- name: Configure storage on webservers
  hosts: webservers
  roles:
    - name: redhat.rhel_system_roles.storage
3.5.
Ejecute el playbook storage.yml.
[student@workstation system-review]$ ansible-navigator run \
> -m stdout storage.yml
PLAY [Configure storage on webservers] *****************************************
...output omitted...
TASK [redhat.rhel_system_roles.storage : make sure blivet is available] ********
changed: [serverb.lab.example.com]
...output omitted...
TASK [redhat.rhel_system_roles.storage : manage the pools and volumes to match the
 specified state] ***
changed: [serverb.lab.example.com]
...output omitted...
TASK [redhat.rhel_system_roles.storage : set up new/current mounts] ************
changed: [serverb.lab.example.com] => (item={'src': '/dev/mapper/apache--vg-
content--lv', 'path': '/var/www', 'fstype': 'xfs', 'opts': 'defaults', 'dump': 0,
 'passno': 0, 'state': 'mounted'})
changed: [serverb.lab.example.com] => (item={'src': '/dev/mapper/apache--vg-logs--
lv', 'path': '/var/log/httpd', 'fstype': 'xfs', 'opts': 'defaults', 'dump': 0,
 'passno': 0, 'state': 'mounted'})
...output omitted...
PLAY RECAP *********************************************************************
serverb.lab.example.com    : ok=21   changed=3    unreachable=0    failed=0   
 skipped=12   rescued=0    ignored=0
4.
Configure el playbook denominado create_crontab_file.yml para ejecutar el grupo de
host webservers que usa el módulo ansible.builtin.cron para crear un sistema de
archivos crontab que programa un trabajo cron recurrente. Cree el archivo /etc/cron.d/
492
RH294-RHEL9.0-es-3-20240620


capítulo 9 |  Automatización de tareas de administración de Linux
disk_usage con el archivo crontab del sistema. Configure el trabajo Cron del sistema de
ese archivo de la siguiente manera:
• Debe ejecutarse con el usuario devops.
• Debe ejecutarse cada dos minutos de 9:00 a 16:59 de lunes a viernes.
• Debe ejecutar el comando df >> /home/devops/disk_usage.
Ejecute el playbook. Debe confirmar que configuró el trabajo de Cron correctamente
después de ejecutar el playbook. Puede ver si el archivo se implementó correctamente o
inspeccionar el archivo de registro /var/log/cron como root para ver si el trabajo se está
ejecutando.
4.1.
Cree un nuevo playbook denominado create_crontab_file.yml y agregue las
líneas necesarias para iniciar el play. El play debe apuntar a los hosts gestionados del
grupo webservers y habilitar el aumento de privilegios.
---
- name: Recurring cron job
  hosts: webservers
4.2.
Defina una tarea que use el módulo ansible.builtin.cron para programar un
trabajo cron recurrente.
  tasks:
    - name: Crontab file exists
      ansible.builtin.cron:
        name: Display disk usage
4.3.
Configure el trabajo para que se ejecute cada dos minutos de las 09:00 a las 16:59 de
lunes a viernes.
        minute: "*/2"
        hour: 9-16
        weekday: 1-5
4.4.
Use el parámetro cron_file para usar el archivo crontab del sistema /etc/
cron.d/disk_usage en lugar del archivo crontab de un usuario individual en /var/
spool/cron/.
Use una ruta relativa para colocar el archivo en el directorio /etc/cron.d.
Si se usa el parámetro cron_file, también debe especificar el parámetro user.
        user: devops
        job: df >> /home/devops/disk_usage
        cron_file: disk_usage
        state: present
4.5.
Al completarse, el playbook debe incluir el siguiente contenido. Revise el playbook para
comprobar la precisión.
---
- name: Recurring cron job
  hosts: webservers
RH294-RHEL9.0-es-3-20240620
493


capítulo 9 |  Automatización de tareas de administración de Linux
  tasks:
    - name: Crontab file exists
      ansible.builtin.cron:
        name: Display disk usage
        minute: "*/2"
        hour: 9-16
        weekday: 1-5
        user: devops
        job: df >> /home/devops/disk_usage
        cron_file: disk_usage
        state: present
4.6.
Ejecute el playbook create_crontab_file.yml.
[student@workstation system-review]$ ansible-navigator run \
> -m stdout create_crontab_file.yml
PLAY [Recurring cron job] ******************************************************
TASK [Gathering Facts] *********************************************************
ok: [serverb.lab.example.com]
TASK [Crontab file exists] *****************************************************
changed: [serverb.lab.example.com]
PLAY RECAP *********************************************************************
serverb.lab.example.com    : ok=2    changed=1    unreachable=0    failed=0   
 skipped=0    rescued=0    ignored=0
5.
Cree un playbook denominado network_playbook.yml para ejecutar el grupo de host
webservers que use el rol redhat.rhel_system_roles.network para configurar la
interfaz de red eth1 con la dirección IP 172.25.250.40/24.
Ejecute el playbook. Confirme que el playbook se haya ejecutado correctamente.
5.1.
Cree un playbook denominado network_playbook.yml y agregue un play orientado
al grupo de hosts webservers.
Incluya el rol redhat.rhel_system_roles.network en la sección roles del play.
---
- name: NIC Configuration
  hosts: webservers
  roles:
    - redhat.rhel_system_roles.network
5.2.
Cree un archivo nuevo denominado network.yml para definir variables de rol.
Como estos valores de variable se aplican a los hosts del grupo de hosts webservers,
debe crear ese archivo en el directorio group_vars/webservers.
Agregue definiciones de variables para soportar la configuración de la interfaz de red
eth1.
El archivo de variable cuenta con el siguiente contenido:
494
RH294-RHEL9.0-es-3-20240620


capítulo 9 |  Automatización de tareas de administración de Linux
[student@workstation system-review]$ vim group_vars/webservers/network.yml
---
network_connections:
  - name: eth1
    type: ethernet
    ip:
      address:
        - 172.25.250.40/24
5.3.
Ejecute el playbook network_playbook.yml para configurar la interfaz de red eth1.
[student@workstation system-review]$ ansible-navigator run \
> -m stdout network_playbook.yml
PLAY [NIC Configuration] *******************************************************
TASK [Gathering Facts] *********************************************************
ok: [serverb.lab.example.com]
...output omitted...
TASK [redhat.rhel_system_roles.network : Configure networking connection profiles]
 ***
changed: [serverb.lab.example.com]
TASK [redhat.rhel_system_roles.network : Show stderr messages] *****************
ok: [serverb.lab.example.com] => {
    "__network_connections_result.stderr_lines": [
        "[002] <info>  #0, state:None persistent_state:present, 'eth1': add
 connection eth1, b2332ada-021d-4f1b-a228-0e342034f95e"
    ]
}
...output omitted...
PLAY RECAP *********************************************************************
serverb.lab.example.com    : ok=10   changed=1    unreachable=0    failed=0   
 skipped=12   rescued=0    ignored=0
Evaluación
Con el usuario student en la máquina workstation, use el comando lab para calificar su
trabajo. Corrija los errores informados y vuelva a ejecutar el comando hasta obtener un resultado
satisfactorio.
[student@workstation ~]$ lab grade system-review
Finalizar
En la máquina workstation, cambie al directorio de inicio de usuario student y use el comando
lab para completar este ejercicio. Este paso es importante para garantizar que los recursos de
ejercicios anteriores no impacten en los siguientes.
RH294-RHEL9.0-es-3-20240620
495


capítulo 9 |  Automatización de tareas de administración de Linux
[student@workstation ~]$ lab finish system-review
496
RH294-RHEL9.0-es-3-20240620


capítulo 9 |  Automatización de tareas de administración de Linux
Resumen
• El módulo ansible.builtin.yum_repository configura un repositorio Yum en un host
gestionado. Para los repositorios que usan claves públicas, puede verificar que la clave esté
disponible con el módulo ansible.builtin.rpm_key.
• Los módulos ansible.builtin.user y ansible.builtin.group crean usuarios y grupos
respectivamente en un host gestionado.
• El módulo ansible.builtin.known_hosts configura hosts conocidos SSH para un servidor
y los módulos ansible.posix.authorized_key configuran claves autorizadas para la
autenticación de usuarios.
• El módulo ansible.builtin.cron configura trabajos Cron del sistema o del usuario en hosts
gestionados.
• El módulo ansible.posix.at configura trabajos at únicos en hosts gestionados.
• La Ansible Content Collection redhat.rhel_system_roles certificada por Red Hat
incluye dos roles de sistema particularmente útiles: storage, que admite la configuración de
volúmenes lógicos LVM, y network, que permite la configuración de interfaces y conexiones de
red.
RH294-RHEL9.0-es-3-20240620
497


498
RH294-RHEL9.0-es-3-20240620


